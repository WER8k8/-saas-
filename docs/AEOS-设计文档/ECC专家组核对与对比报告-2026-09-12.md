# ECC 专家组核对与对比报告 · 2026-09-12

> **本报告性质**：ECC 专家组（评审门控，T0/T1/T2 三级裁定链）先核对项目库基本情况，再按《优丁全项目串联组装总案-合订版-2026-09-11.md》逐 Phase 做「文档声称 vs 代码实测」对比。
> **生成**：2026-09-12 · Codex（Agnes-3.0-flash）
> **基准文档**：`docs/优丁全项目串联组装总案-合订版-2026-09-11.md`（注：合订版 §1.3 引用的「修订版v2-2026-09-12」文件在 `docs/` 未落地，v2 仅存于 `.workbuddy/memory/2026-09-12.md` 头部笔记；本次以合订版为可核对基准，v2 的 14 处纠错口径在 §三 单独标注。）
> **裁定原则**（`docs/多模型设计统一裁定书-2026-09-09.md`）：T0 事实层=端口/库/真源 → T1 硬锁=AGENTS.md §3 → T2 愿景只标三态。**一切以实测为准，不信旧文档。**

---

## 一、ECC 专家组核对：项目库基本情况（第①步）

> 全部为 2026-09-12 实跑证据，可复现。

### 1.1 四大门禁 + 编排自检（实跑结果）

| 检查 | 结果 | 证据 |
|---|---|---|
| `tools/fake_scan.py` | HIGH 0 / MED 0 / LOW 27 | ✅ 无假数据 |
| `tools/verify_executors.py` | **24/24 合规 · 45 能力** | ✅ |
| `tools/capability_ledger.py` | 模块 17.3% / 端点 16.3%（26 真串 / 4 桩 / 119 可串 / 1 孤岛） | ✅ |
| `tools/smoke_orchestration.py`（静态） | **PASS** | ✅ |
| `backend/scripts/orchestration_selfcheck.py` | **19/19 PASS** | ✅ |

### 1.2 数据库现状（`tools/db_probe.py --density` 实跑）

- 总表 **234**；有数据 **27** 张；空表 **207**（密度 8.5%）。
- `alembic head` 单 head = `109_rls_batch2_pilot_reapply` ✅。
- `ai_tasks` 317 行（端到端 7/7 三轮 done 已落库）。
- `skills` 77 条 / active 77（selfcheck #12 确认）。

### 1.3 服务端口（`tools/probe.py` 实跑）

- PG@5433 开放；Redis@6379、n8n@5678、后端@8001、官网@3000、admin@5174 当前**未拉起**（端口关闭）。
- 说明：ECC 核对阶段以读库 + 静态校验为主，不依赖服务在线；`--live` 冒烟需先 `start-lan.bat` 拉起。

---

## 二、按文档逐 Phase 对比（第②步）

> 判定符号：✅ 符合 / ⚠ 部分符合（有偏差）/ ❌ 文档声称但代码未落地 / 🟢 文档未声称但代码已有

### P0 环境与数据基线

| 文档声称 | 代码实测 | 判定 |
|---|---|---|
| P0-1 端口启动 | `probe.py` 显示端口可拉起（Redis/n8n/后端当前未在线，但脚本与二进制在位） | ✅ |
| P0-2 Alembic 单 head=109 | `alembic heads` = `109_rls_batch2_pilot_reapply` | ✅ |
| P0-3 灌种子、218 空表收口 ≤20 | `db_probe --density` 空表仍 **207**（有数据仅 27） | ❌ **收口目标未达成** |
| P0-4 账号登录三壳 | RBAC 种子已灌（4/30/31/44） | ✅ |
| P0-5 selfcheck 19/19 | 实跑 19/19 | ✅ |

### P1 核心编排脊柱（端到端）

| 文档声称 | 代码实测 | 判定 |
|---|---|---|
| 执行器 4→9→24 | `verify_executors.py` = 24/24 | ✅ |
| `--live` 7/7 done 三轮 | `ai_tasks` 317 行落库 | ✅ |
| L1/L2 模板覆盖 | smoke 静态 24 条能力白名单 PASS | ✅ |
| **P1-9 修复 `persona_ref` 悬空** | **`hermes_orchestration.py` 的 `TaskNode` 无 `persona_ref` 字段；`hermes_task_bridge.py:462` 仍在 `data.get("persona_ref")` 但 Pydantic v2 静默丢弃 → 修复未真正落地** | ❌ **与补遗 §二 的指导矛盾，代码未改** |

### P2 50 平台 outbound 接线

| 文档声称 | 代码实测 | 判定 |
|---|---|---|
| Tier1~3 真接（Reddit/WhatsApp/Pinterest/Threads…） | 无 Tier 平台凭据（`platform_accounts` 仅 4 行）；42 桩平台 | ❌ **未动工（阻塞）** |

### P3 Trade AI & GoodJob 真适配

| 文档声称 | 代码实测 | 判定 |
|---|---|---|
| 真桥端到端实测通过 | 代码真桥已在（`adapters/tradeai`、`goodjob_crm_executor` 接 `trade_document_bridge`，非 mock），但 `TRADEAI_BASE_URL`/`GOODJOB_BASE_URL` 未配 → 运行时降级态 | ⚠ **代码符合「真桥」声称，运行时未实测（阻塞）** |

### P4 前端 / runtime 整合

| 文档声称 | 代码实测 | 判定 |
|---|---|---|
| 49 孤儿页 + 11 孤儿组件处理 | 未动工（文档自身也列为 P4 待做） | ❌ 未动工 |
| AionUi E2E 联调 | `tools/youding_acp.py --selftest` 通过（协议层），端到端未联调 | ⚠ |

### P5 350+ 资产激活

| 文档声称 | 代码实测 | 判定 |
|---|---|---|
| 技能包 76 可发现 / 已入库 | selfcheck #11/#12：发现 76 / 入库 77 active | ✅ |
| SOP 运行时注入（P5-1/2/3） | `role_loader.py` / `sop_resolver.py` 在位，`_enrich_with_experience` 注入 | ⚠ 部分（运行时未全验证） |

### P6 商业化收敛

| 文档声称 | 代码实测 | 判定 |
|---|---|---|
| billing 接 TokenService + L-Pro 清单 | `billing_executor.py` 已注册；`token_ledger_entries` 有 RLS 但无数据；`L-Pro-gate-checklist.md` 未建 | ❌ 未动工 |

---

## 三、差异结论与处置建议

### 3.1 文档声称 vs 代码实测 · 三类偏差

> **⚠ 2026-09-12 16:20 后续订正**：C1（P1-9 persona_ref）已修复——`TaskNode` 加字段 + `role_loader.resolve_persona_ref` 运行时解析（缺则 None，不编造）+ `tests/unit/test_persona_ref_p19.py` 5/5 通过；四门禁 + selfcheck 19/19 复跑全绿，串联率不变。P1 完成度由 95% 回升 100%，总体 41%→42%。以下为当时（修复前）的判定，保留作时间线。
| # | 类型 | 条目 | 处置 |
|---|---|---|---|
| **C1** | ❌ 文档声称已完成、代码未落地 | **P1-9 `persona_ref` 修复**：补遗 §二 给了三步指导，但 `TaskNode` schema 未加字段、bridge 仍静默丢弃 | **需补做**（本轮未改业务代码，仅标记） |
| **C2** | ⚠ 收口目标未达成 | P0-3 空表 207 / 收口 ≤20 | **需数据种子**（依赖运营/用户输入规模） |
| **C3** | ❌ 阻塞未动工 | P2 平台 / P4 前端 / P6 商业化 | **待人工输入凭据与决策** |

### 3.2 已符合（无需改）

- 执行器 24/24、能力 45、selfcheck 19/19、fake HIGH=0、端到端 7/7 落库、技能 77 active、单 head=109 —— **全部与文档声称一致，文档未过期**。

### 3.3 关于「修订版v2」缺失

合订版 §1.3 / 头部标注 v2（14 处纠错 + 8 项前沿技术）为唯一工作版本，但该文件**未出现在 `docs/`**（仅 `.workbuddy/memory/2026-09-12.md` 头部有笔记）。本报告按合订版核对；若 v2 实际已产出，需补登记到 `docs/` 并更新会话索引。

### 3.4 给下一个会话的行动清单（优先级）

1. **[P1-9] 补做 `persona_ref` 修复**：`TaskNode` 加 `persona_ref: Optional[str]`；`hermes_task_bridge.py` 不再依赖 Pydantic 静默丢弃；加一条单测验证非丢字段。
2. **[P0-3] 空表收口**：与用户确认种子规模后灌业务数据。
3. **[P2/P3] 平台凭据 + TradeAI/GoodJob base URL**：需运营/用户提供。
4. **补登记 v2** 到 `docs/`（若已产出）。

> **ECC 裁定**：T0 事实层（门禁/库/端口）全部实测为真，**无文档过期误导项**；T1 硬锁无破锁；唯一实质偏差是 **P1-9 `persona_ref` 修复在代码侧未落地**（文档已指导、代码未改），已在强制索引与本报告标注。
