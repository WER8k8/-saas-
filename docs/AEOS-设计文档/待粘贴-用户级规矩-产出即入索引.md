# 待粘贴到用户级全局规则的文本

> **为什么需要你手动粘贴**：用户级全局规则文件位于 `C:\Users\Administrator\.claude\CLAUDE.md`，
> 该路径被系统判定为**受保护位置**，我无法挂载或写入（已实测确认，非权限问题而是硬限制）。
>
> **已落地的替代路径**（这两个我已写入）：
> 1. 我的用户级跨会话记忆 —— 已立为 feedback 类规矩
> 2. 项目级 `CLAUDE.md` —— 本项目所有 AI 均受约束
> 3. 项目侧长期记忆 `.workbuddy/memory/MEMORY.md` —— 其他 IDE 也读得到
>
> **若你要真正全局生效**（跨所有项目、所有 AI），把下面这段追加到 `~/.claude/CLAUDE.md`。

---

## 复制以下内容 ↓

```markdown
## 产出即入索引（项目协作硬规）

在「上线网站开发完成」（优丁 YouDing）项目内，**任何一轮产生新文件后，必须更新会话产出总索引**，
否则视为交付未完成。

**索引**：`docs/00-会话产出串联索引-<最新日期>.md`（同步至 Obsidian `01-权威文档与系统全景/`）

**每轮收尾五步**：
1. 更新索引「会话脉络」表 —— 新增「问了什么 → 得出什么结论 → 产出哪个文件」
2. 更新索引「产出物总表」—— 新文件挂到对应分组
3. 新增可重跑脚本 → 更新「可重跑的工具」表并标「何时跑」
4. **查漏**：`docs/*<日期>*.md` + `tools/*.py` + `tools/*.bat` 逐个比对，**未收录数必须为 0**
5. 若本轮**推翻旧结论**，在索引里明确标出

**为什么**：索引一旦没人维护，会比普通文档**更误导人** —— 它看起来像是「全的」。
**与强制索引的分工**：《强制索引-必读》讲「事实是什么」；会话索引讲「事实怎么变来的、还变过什么」。
**无新产出时**：也要留痕（注明本轮无新文件），避免后来者误判为漏记。

**适用范围**：所有在本项目内工作的 AI / IDE / 智能体，不受工具与厂商限制。
```

## 复制以上内容 ↑

---

*本文件由 Claude 于 2026-09-10 生成，供用户手动粘贴至用户级全局规则。*
