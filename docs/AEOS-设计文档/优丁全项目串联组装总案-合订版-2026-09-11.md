# 优丁全项目串联组装总案（合订版）· 2026-09-11> **📌 2026-09-12 起本合订版已被《优丁全项目串联组装总案-修订版v2-2026-09-12.md》取代（14 处纠错 + 前沿技术评估）——后续修订以 v2 为唯一工作版本，本册为历史存档。**> **文件性质**：本日三份设计文档的合订本——① 总体装配方案（正文）② 逐项实测复核勘误（订正层）③ Obsidian 第二大脑同步架构设计（知识层）> **合并原因**：指挥官指令「上面这三个文档写成一个」。三册内容零删改合并，仅统一头部与跨册引用；分册原件保留并已标注"并入合订版"。> **阅读顺序**：上篇（装配总方案）→ 中篇（复核勘误，读方案前先看勘误，以勘误为准）→ 下篇（Obsidian 同步设计与历史回顾结论）。> **合订时点**：2026-09-11 19:19；合订版含全部已落地订正（head=109 / 执行器 24 / TradeAI·GoodJob 真桥已接待联调等）。---# 上篇 · 优丁全项目串联组装总方案---## 1. 执行摘要（TL;DR）### 1.1 当前状态一句话**零件齐了、引擎已点火、轮子只接了 1/6、数据是空的。**- **零件齐了**：45,350 源码文件、8 大子系统、24 个 Hermes 执行器、150 个路由模块、50 个平台台账、350+ 智能资产全部在位。- **引擎已点火**：Hermes 编排主链已于 2026-09-11 三轮端到端真跑通（7/7 节点 done），`POST /api/v1/orchestration/tasks/from-intent` 可一句自然语言生成 TaskGraph 并自动派发。- **轮子只接了 1/6**：模块级串联率仅 **17.3%**（26/150 已串 / 119 待串 / 4 桩 / 1 孤岛），按端点口径 16.3%。- **数据是空的**：229 张表仅 11 张有数据，218 张空表导致一切"真跑"缺业务上下文。### 1.2 组装目标关闭端到端闭环：```租户意图（AionUi/Web/Admin）  → DeepSeek Harness / planner.decompose() 拆解为 TaskGraph  → Hermes DAG 调度 + Paperclip 审批闸口  → 24 个执行器（site/content/publish/nurture/egress/seo/lead/…）  → n8n/Browser Runtime 出站取证  → 证据回写 AiTask.output_json  → 租户站点 / CRM / 物流系统可见  → 形成「询盘→报价/PI→定金→生产→ shipping docs→尾款→物流可查」外贸 7 步闭环```### 1.3 关键指标| 指标 | 当前值 | 目标值 | 来源 ||---|---:|---:|---|| 模块串联率 | 17.3% | ≥75% | `tools/capability_ledger.py` || 后端未串模块 | 119 / 150 | ≤16 backoffice + 1 deprecated | `未驱动串联资产盘点-2026-09-11.md` || 桩平台 | 42 / 50 | 按需 Tier1~Tier3 真接 | `50平台接线台账-2026-09-10.md` || 空数据表 | 218 / 229 | ≤20（系统/审计表可空） | `资产盘点审计-全维度-2026-09-11.md` || 孤儿页/组件 | 60 / 89 | 0（并入主站或归档） | `未驱动串联资产盘点-2026-09-11.md` || 前端测试文件 | 0 | ≥1 基线套件 | `资产盘点审计-全维度-2026-09-11.md` || 硬编码凭证 | ~32 处 | 0 | `资产盘点审计-全维度-2026-09-11.md` || 假货/占位 | ~308 处 | HIGH=0 / MED=0 | `tools/fake_scan.py` || TradeAI/GoodJob 适配器 | 代码已接真桥，**运行时未验证**（`simulated:true` 判定基于桥未配置的降级态；代码级 mock 已于 09-10 移除） | 真桥端到端实测通过 | `编排主链打通实作记录-2026-09-10.md` + `组装总方案复核补遗-2026-09-11.md` || AionUi ACP 桥 | 协议自测通过 | 端到端联调通过 | `AionUi接入评估-2026-09-10.md` |---## 2. 当前状态快照（量化）### 2.1 家底基数| 维度 | 数量 | 来源 ||---|---:|---|| 源码文件 | 45,350 / ~950 万行 | `资产盘点审计-全维度-2026-09-11.md` §一 || 工作区总文件（含制品） | 642,932 / 12.6 GB | `审计复核确认-百维度范围与拓扑-2026-09-11.md` §S1 || 已审计源码占排除制品后比例 | 31.3% | `审计复核确认-百维度范围与拓扑-2026-09-11.md` §S16 || 代码地图细读文件 | 4,608 | `审计复核确认-百维度范围与拓扑-2026-09-11.md` §B7 || 内部依赖边 | 3,050 | `代码地图/topology.json` || FastAPI 路由 | 1,895 | `代码地图/topology.json` || ORM 表 | 237~255（统计口径差异） | `资产盘点审计` / `代码地图` |### 2.2 关键健康指标| 指标 | 值 | 严重度 | 来源 ||---|---|---|---|| 串联率 | 17.3% | 🔴 | `tools/capability_ledger.py`（实作记录 §二十二） || 未驱动后端模块 | 119 | 🔴 | `未驱动串联资产盘点-2026-09-11.md` §五 || 桩平台 | 42 | 🔴 | `50平台接线台账-2026-09-10.md` §三 || 空表 | 218 | 🔴 | `资产盘点审计-全维度-2026-09-11.md` §5.2 || 孤儿页 + 组件 | 60 | 🟠 | `未驱动串联资产盘点-2026-09-11.md` §五 || 前端测试文件 | 0 | 🔴 | `资产盘点审计-全维度-2026-09-11.md` §F4 || 硬编码凭证 | ~32 | 🟠 | `资产盘点审计-全维度-2026-09-11.md` §QR4 || 假货命中 | ~308 | 🟠 | `资产盘点审计-全维度-2026-09-11.md` §QR5 || 绕行编排引擎 | 5 → 0（H 段已收口） | ✅ | `编排主链打通实作记录-2026-09-10.md` §十八 || 编排主链端到端 | 7/7 done 三轮复现 | ✅ | `编排主链打通实作记录-2026-09-10.md` §十四 |### 2.3 AionUi 口径订正原 `未驱动串联资产盘点-2026-09-11.md` 把 AionUi 计为「1 个孤立外部项目」。经复核：- AionUi 已通过 `tools/youding_acp.py`（ACP 服务端，`--selftest` 通过）桥接后端 `POST /api/v1/orchestration/tasks/from-intent`。- 代码地图显示 AionUi 66 个文件、48 条内部 import 边，与主仓零代码依赖，但**设计层已串联**。- **新口径**：AionUi = 已串联（ACP 桥·端到端未联调），孤立外部项目由 1 → **0**。---## 3. 装配原则（硬约束）以下约束来自 `AGENTS.md` §3 与仓库级契约，装配过程中**不可破坏**：| 锁ID | 约束内容 | 违反后果 ||---|---|---|| **LOGIN-LOCK-01** | 唯一登录路由 `/login`，唯一组件 `frontend/admin/src/views/login/index.vue` | 高优先级回归 || **ROLE-SHELL-LOCK-01** | 壳固定：租户 `/client/*`、超管 `/admin`、区域代理 `/partner`、市级代理 `/agent/*` | 高优先级回归 || **DESIGN-TOKEN-LOCK-01** | 平台主色 = 薄荷绿 `#4a9b8c`；禁止硬编码 `bg-[#2563eb]`/`#7c3aed`/`#ea580c`；禁止恢复暗色切换 | 高优先级回归 || **ENV-LOCK-01** | 真实数据库 = PG 15.8 @5433（单 head 109）、Redis 5.0 @6379、n8n @5678；禁止随意切回 SQLite | 掩盖迁移问题 || **SYSTEM-LOCK-02** | 8 大子系统不可裁减：DeepSeek Harness、Hermes、Site/Calc/BOQ、DeerFlow、Trade AI Agent、GoodJob CRM、350+ 资产、Browser Runtime/n8n | 架构违法 || **开发方式** | 禁止手敲 `uvicorn`；一律走 `start-lan.bat` / `start-dev-admin.ps1` | 易漏 `REDIS_ENABLED=false` || **归档纪律** | 不恢复 `_archive/` 或旧备份的能力；代码树内不放备份 | 历史债务复发 || **不假交付** | 无真引擎时标 `degraded`/`simulated:true`，不静默假成功 | 信任破产 || **编排门禁** | 任何编排改动必须通过 `backend/scripts/orchestration_selfcheck.py` 19 项全绿 | 质量回归 || **外部访问** | 外部访问只用 LAN 模式（`start-lan.bat` 绑 `0.0.0.0`），不用 127.0.0.1 | 联机失败 |---## 4. 装配目标态（Target Architecture）### 4.1 八大子系统互联图```mermaidflowchart TB    subgraph OUTER["外层入口"]        A1["AionUi 桌面客户端<br/>ACP http/stdio"]        A2["Web 官网 3000<br/>Vite 暖白版"]        A3["Admin 后台 5173"]    end    subgraph HARNESS["L1 认知层"]        H1["DeepSeek Harness<br/>stdio JSON-RPC"]        H2["planner_service.decompose()<br/>模板+LLM 混合拆解"]    end    subgraph HERMES["L2 调度层"]        I1["DAG Governor<br/>环路/上限/JsonPath 校验"]        I2["Hermes 状态机<br/>ai_tasks + hermes_plan/hermes_node"]        I3["Paperclip 审批闸口<br/>wait_human → 批准/拒绝"]        I4["节点终态钩子<br/>Evolution + Pipeline"]    end    subgraph EXEC["执行层 · 24 执行器"]        E1["site_builder / content / publish / nurture / egress"]        E2["deerflow / seo / media / lead / engagement"]        E3["forum / research / order / logistics / browser"]        E4["ai_engine / billing / accio / ubrain / wangcai"]    end    subgraph EXTERNAL["外部子系统 · adapter"]        X1["Trade AI Agent<br/>社媒拓客/WhatsApp"]        X2["GoodJob CRM<br/>7步履约/PI/CI/PL"]    end    subgraph DELIVERY["取证与出站"]        D1["n8n @5678"]        D2["Browser Runtime<br/>Disk JSONL + DB"]    end    subgraph ASSETS["350+ 资产"]        S1["Skills 76"]        S2["AgencyZH 319"]        S3["ECC 286 SOP"]    end    subgraph COMMERCIAL["商业计费"]        C1["Token Wallet"]        C2["SaaS 订阅"]        C3["静态 IP/指纹槽位"]    end    A1 -->|ACP| H2    A2 -->|HTTP API| H2    A3 -->|HTTP API| H2    H1 -->|reasoning| H2    H2 -->|TaskGraph| I1    I1 --> I2    I2 -->|dispatch| EXEC    I2 -->|approval| I3    I2 -->|hooks| I4    EXEC -->|adapter| X1    EXEC -->|adapter| X2    EXEC -->|webhook| D1    EXEC -->|evidence| D2    EXEC -->|sop_ref| ASSETS    EXEC -->|ledger| C1    C1 --> C2    C1 --> C3```### 4.2 外贸 7 步闭环| 步骤 | 业务动作 | 承载执行器/模块 | 现状 ||---|---|---|---|| 1 | 询盘捕获 | `inquiry` executor + `engagement` executor | ✅ 已注册 || 2 | 需求核算/报价 | `ai_engine` + `cross_border` | 🟡 待接入模板 || 3 | PI（形式发票） | `goodjob_crm` executor（真桥） | 🟠 代码已接真桥（09-10 去假），**运行时未实测**（`GOODJOB_BASE_URL` 未配即诚实 failed） || 4 | 定金核销 | `billing` executor + `wallet` | 🟡 表已建、待 seed || 5 | 生产跟单 | `order` executor + `manufacturing` | 🟡 表在、缺订单路由 || 6 | 发运单证（CI/PL） | `goodjob_crm` executor | 🟠 同上：真桥已接、待端到端实测 || 7 | 尾款 + 物流可查 | `logistics` executor + `wallet` | 🟡 部分 |### 4.3 AionUi 的角色定位- **不是**嵌进 Web Admin 的组件（AionUi 是 Electron 桌面应用）。- **是**可选的「租户外层桌面壳」：通过 ACP 协议连接优丁后端 `from-intent`。- 形态决策：独立伴侣应用（租户/运营者另装客户端），或仅作为插件参考设计自建 Web 外层。---## 5. 分阶段装配路线图（Phase-by-phase Assembly Plan）### Phase 0：环境 & 数据基线| 项 | 内容 ||---|---|| **目标** | 让所有服务可启动、数据库有最小业务上下文、开发账号就位 || **入口条件** | 代码已在 worktree；PG/Redis/n8n 可启动；`start-lan.bat` 可用 || **任务** | 1) 起 PG/Redis/n8n；2) 跑 `alembic upgrade head`（单 head=109）；3) 跑 `backend/seed.py` 灌种子；4) `seed_dev_users.py` 补账号；5) 确认 218→≈50 空表 || **出口条件** | `GET /api/v1/health` 200；`db_probe.py` 显示业务表有≥20 张非空；`admin/tenant/agent` 账号可登录 || **验证命令** | `python tools/probe.py`；`python tools/db_probe.py --density`；`python backend/scripts/orchestration_selfcheck.py` || **涉及文件** | `backend/seed.py`, `backend/alembic_migrations/versions/*`, `tools/seed_dev_users.py`, `start-lan.bat`, `config/dev/.env` |### Phase 1：核心编排脊柱 end-to-end（已 80% 完成，补剩余）| 项 | 内容 ||---|---|| **目标** | 让 `decompose()` → `TaskGraph` → 24 执行器 → 回写 → n8n 这条链对所有 capability 可跑 || **入口条件** | Phase 0 通过；`smoke_orchestration.py --live` 已 7/7 done || **任务** | 1) 把 112 个 `to-orchestrate` 模块按业务步封装为执行器（优先 10 个已真服务）；2) 更新 L1/L2 模板引用新 capability；3) 补 `order` 路由；4) 修复 `persona_ref` 悬空 || **出口条件** | `smoke_orchestration.py --live --dispatch --approve` 跑通 ≥10 节点；`verify_executors.py` 24/24；`capability_ledger.py` 串联率 ≥40% || **验证命令** | `python tools/smoke_orchestration.py --live --dispatch --approve --timeout 300`；`python tools/verify_executors.py`；`python tools/capability_ledger.py` || **涉及文件** | `backend/app/services/hermes/executors/*.py`, `backend/app/services/hermes/planner_service.py`, `backend/app/api/v1/routes/orchestration.py`, `backend/app/services/tasks/hermes_task_bridge.py` |### Phase 2：50 平台 outbound 接线| 项 | 内容 ||---|---|| **目标** | 把 42 个桩平台按 tier 链分批接入真发布/养号/隔离环境 || **入口条件** | Phase 1 通过；`publish` executor 已真；`n8n` 2 webhook 激活 || **任务** | 1) Tier1：Reddit/WhatsApp/Pinterest/Threads（aitoearn 已支持，改 tier 链）；2) Tier2：TikTok/抖音/快手/B站/视频号（sau/biliup）；3) Tier3：小红书/百家号/微博等文本适配平台；4) Tier4/Tier5 区域社媒与 B2B 电商 || **出口条件** | 每批平台 `publish.multi` 实测真发或诚实 `not_configured`；养号规则 + browser_profile + egress 槽位三件套配齐 || **验证命令** | `python tools/capability_ledger.py --platforms`；抽查 `POST /api/v1/publish/distribute`；`db_probe.py` 查 `platform_tenant_origins` || **涉及文件** | `backend/app/services/platform_catalog.py`, `backend/app/services/publish_workers/tier_router.py`, `backend/app/services/social_nurture_service.py`, `backend/app/services/egress_*.py` |### Phase 3：Trade AI Agent & GoodJob CRM 真适配| 项 | 内容 ||---|---|| **目标** | 让社媒拓客与 CRM 7 步履约的**真桥端到端跑通**（代码已接桥，本阶段做配置+联调+实测），全面进入 Hermes 执行面 || **入口条件** | Phase 1 通过；`_external/trade-ai-agent` 与 `_external/goodjob-crm` 已存在 || **任务** | 1) 配置 `TRADEAI_BASE_URL`/`GOODJOB_BASE_URL`；2) 在 `adapters/tradeai/` 与 `goodjob_executor.py` 实现 HTTP 桥；3) 失败时诚实返回 `failed` 与原因；4) 补全 7 步单证生成（PI/CI/PL） || **出口条件** | `pytest tests/test_aeos_full_loop_dag.py` 通过；GoodJob 桥未配置时不静默成功；TradeAI 未注册 workflow 时明确 failed || **验证命令** | `python -m pytest tests/test_aeos_full_loop_dag.py -v`；`python tools/fake_scan.py` HIGH=0 || **涉及文件** | `backend/app/services/adapters/tradeai/*.py`, `backend/app/services/hermes/executors/trade_ai_agent_executor.py`, `backend/app/services/hermes/executors/goodjob_crm_executor.py`, `backend/app/services/goodjob/trade_document_bridge.py` |### Phase 4：前端/runtime 整合| 项 | 内容 ||---|---|| **目标** | 修复 49 孤儿页/11 孤儿组件，统一 API 客户端，完成 AionUi ACP 端到端联调 || **入口条件** | Phase 1 通过；3000 官网 Vite 运行源可启动 || **任务** | 1) 49 孤儿页：并入主站路由或归档 `_archive/`；2) 11 孤儿组件：Mobile* 组件接入响应式布局、seo 组件接入页面；3) axios 客户端对齐 `api/orchestration.ts`；4) AionUi 补工作树 + 装扩展 + 跑 ACP 会话 || **出口条件** | 前端 `npm run typecheck && npm run lint && npm test` 通过；AionUi `--selftest` 外再跑一次真实 `from-intent` || **验证命令** | `cd frontend/admin && npm run typecheck && npm run lint && npm test`；`cd "主要备份/上线网站/frontend" && npx vite --port 3000`；AionUi 扩展跑 `initialize` + `prompt` || **涉及文件** | `frontend/admin/src/router/index.ts`, `frontend/admin/src/views/**/*.vue`, `frontend/admin/src/api/*.ts`, `tools/aionui-extension-youding/`, `_external/aionui/` |### Phase 5：350+ 资产激活| 项 | 内容 ||---|---|| **目标** | 让 Skills 76、AgencyZH 319、ECC 286 在执行器运行时被消费 || **入口条件** | Phase 1/3 通过；`sop_ref` 字段已存在 || **任务** | 1) 在 `site_builder`/`content`/`deerflow`/`publish` 等执行器运行时注入 `sop_ref`；2) 默认关闭 ECC 匹配（保质量），按租户行业开启；3) 角色加载器接入 `accio`/`research` executor || **出口条件** | 跑一条 intent 后 `evolution_task_records` 与 `sop_refs` 有记录；`skills` 表被查询 || **验证命令** | `python tools/capability_ledger.py`；抽查 DB `sop_refs` / `evolution_task_records` || **涉及文件** | `backend/app/services/hermes/executors/*.py`, `backend/app/services/hermes/agency/role_loader.py`, `backend/app/services/registry/skill_pack_loader.py`, `_external/ecc/` |### Phase 6：商业化收敛| 项 | 内容 ||---|---|| **目标** | Token 钱包、SaaS 订阅、静态 IP 槽位三轨计费真正拦截/扣费 || **入口条件** | Phase 1/5 通过；`wallet_accounts`/`token_ledger_entries` 等 5 表已补 RLS || **任务** | 1) `billing` executor 调用 TokenService；2) `model_gateway` 记账写入 `model_call_ledger`；3) `egress` executor 分配槽位并计费；4) L-Pro 门禁清单落地 || **出口条件** | 每次 AI 调用后 `token_ledger_entries` 有记录；余额不足时 `billing` 拦截返回 `budget_exceeded` || **验证命令** | `python -m pytest tests/unit/test_billing_executor.py`；`python tools/fake_scan.py`；L-Pro 检查清单 || **涉及文件** | `backend/app/services/billing/*.py`, `backend/app/services/model_gateway.py`, `backend/app/services/hermes/executors/billing_executor.py`, `backend/app/services/hermes/executors/egress_executor.py` |---## 6. 关键路径（Critical Path）```mermaidgraph LR    P0["Phase 0<br/>环境+数据基线"] --> P1["Phase 1<br/>编排脊柱 end-to-end"]    P1 --> P2["Phase 2<br/>50平台接线"]    P1 --> P3["Phase 3<br/>TradeAI/GoodJob真桥"]    P1 --> P4["Phase 4<br/>前端/runtime整合"]    P1 --> P5["Phase 5<br/>350+资产激活"]    P3 --> P6["Phase 6<br/>商业化收敛"]    P5 --> P6```**关键路径说明**：1. **Phase 0 是所有后续阶段的前提**——没有种子数据，执行器跑不出真实 output，`input_from` JsonPath 无数据可传。2. **Phase 1 是瓶颈**——必须在 24 个执行器都注册且可被 planner 引用后，Phase 2/3/4/5 才能并行展开。3. **Phase 3（GoodJob）必须在 Phase 6 之前**——没有真实 CRM 桥，商业化计费的「履约单证」链路无法闭环。4. **Phase 5 与 Phase 2/3/4 可并行**——资产注入不依赖平台接线或前端整合。5. **Phase 6 最后**——必须在所有消耗 Token/槽位的执行器都真跑后，才能验证计费拦截。---## 7. 每个阶段的详细任务列表### Phase 0 任务| 任务ID | 描述 | 文件/模块 | 依赖 | 验证 | 优先级 ||---|---|---|---|---|---|| P0-1 | 启动 PG/Redis/n8n 并确认端口 | `scripts/start-dev-admin.ps1`, `start-lan.bat` | 无 | `tools/probe.py` 全绿 | P0 || P0-2 | 跑 Alembic 升级到单 head=109 | `backend/alembic_migrations/versions/109_rls_batch2_pilot_reapply.py` | P0-1 | `alembic heads` 单 head | P0 || P0-3 | 灌业务种子数据，重点填 218 空表 | `backend/seed.py`, `tools/seed_dev_users.py` | P0-2 | `db_probe.py --density` 业务表非空≥20 | P0 || P0-4 | 验证账号登录三壳 | `config/dev/.env` | P0-3 | `/login` + `/admin` + `/client/today` 可登录 | P0 || P0-5 | 跑通 orchestration_selfcheck 19 项 | `backend/scripts/orchestration_selfcheck.py` | P0-4 | 19/19 PASS | P0 |### Phase 1 任务| 任务ID | 描述 | 文件/模块 | 依赖 | 验证 | 优先级 ||---|---|---|---|---|---|| P1-1 | 封装 `media` executor（图片/视频素材） | `backend/app/services/hermes/executors/media_executor.py` | P0 | `verify_executors.py` 25/25 | P0 || P1-2 | 封装 `seo` executor | `backend/app/services/hermes/executors/seo_executor.py` | P0 | smoke 含 seo 节点 | P0 || P1-3 | 封装 `lead` executor | `backend/app/services/hermes/executors/lead_executor.py` | P0 | lead capability 可派发 | P0 || P1-4 | 封装 `research` executor | `backend/app/services/hermes/executors/research_executor.py` | P0 | research 背调节点 done | P0 || P1-5 | 封装 `engagement` executor（互动获客） | `backend/app/services/hermes/executors/engagement_executor.py` | P0 | engagement 节点 done | P0 || P1-6 | 封装 `forum` executor | `backend/app/services/hermes/executors/forum_executor.py` | P0 | forum 节点 done | P1 || P1-7 | 封装 `order` executor + 补 `orders` 路由 | `backend/app/services/hermes/executors/order_executor.py`, `backend/app/api/v1/routes/orders.py` | P0 | orders 表可写 | P1 || P1-8 | 更新 planner L1/L2 模板覆盖新业务步 | `backend/app/services/hermes/planner_service.py`, `backend/app/services/hermes/templates/*.py` | P1-1~P1-7 | 不同 intent 产出不同 TaskGraph | P0 || P1-9 | 修复 `persona_ref` 悬空 | `backend/app/schemas/hermes_orchestration.py`, `backend/app/services/tasks/hermes_task_bridge.py` | P0 | pydantic extra 不丢字段 | P1 |### Phase 2 任务| 任务ID | 描述 | 文件/模块 | 依赖 | 验证 | 优先级 ||---|---|---|---|---|---|| P2-1 | Tier1 平台接线：Reddit/WhatsApp/Pinterest/Threads | `backend/app/services/platform_catalog.py`, `publish_workers/tier_router.py` | P1 | 实测分发到平台返回真响应或 `not_configured` | P0 || P2-2 | Tier2 视频平台接线：TikTok/抖音/快手/B站/视频号 | 同上 + `social_nurture_service.py` | P2-1 | 视频分发节点 done | P1 || P2-3 | Tier3 国内图文平台接线 | 同上 | P2-1 | 图文分发节点 done | P1 || P2-4 | 每平台配养号规则 + browser_profile + egress IP | `backend/app/services/social_nurture_service.py`, `backend/app/models/platform_tenant_origins.py`, `egress_endpoints` | P2-1 | DB 有对应记录 | P0 || P2-5 | 更新 `capability_ledger.py` 平台串联率 | `tools/capability_ledger.py` | P2-1~P2-4 | 平台串联率≥50% | P1 |### Phase 3 任务| 任务ID | 描述 | 文件/模块 | 依赖 | 验证 | 优先级 ||---|---|---|---|---|---|| P3-1 | 配置 TradeAI/GoodJob 环境变量与 base URL | `config/dev/.env`, `.env.example` | P0 | 环境变量非空 | P0 || P3-2 | 实现 TradeAI HTTP 适配器 | `backend/app/services/adapters/tradeai/*.py` | P3-1 | TradeAI 桥健康检查 200 | P0 || P3-3 | 实现 GoodJob HTTP 桥（PI/CI/PL） | `backend/app/services/goodjob/trade_document_bridge.py` | P3-1 | GoodJob 桥健康检查 200 | P0 || P3-4 | `trade_ai_agent_executor` 接真适配器 | `backend/app/services/hermes/executors/trade_ai_agent_executor.py` | P3-2 | 产生真实线索或诚实 failed | P0 || P3-5 | `goodjob_crm_executor` 接真桥 | `backend/app/services/hermes/executors/goodjob_crm_executor.py` | P3-3 | 生成真实 PI/CI/PL 或诚实 failed | P0 || P3-6 | 跑 AEOS 全闭环集成测试 | `backend/tests/test_aeos_full_loop_dag.py` | P3-4/P3-5 | 测试通过 | P0 |### Phase 4 任务| 任务ID | 描述 | 文件/模块 | 依赖 | 验证 | 优先级 ||---|---|---|---|---|---|| P4-1 | 处理 49 孤儿页：路由挂载或归档 | `frontend/admin/src/router/index.ts`, `_archive/` | P0 | router 无未注册页面 | P1 || P4-2 | 处理 11 孤儿组件：接入或归档 | `frontend/components/**/*.vue`, `frontend/admin/src/components/**/*.vue` | P0 | 组件被至少一处引用或归档 | P1 || P4-3 | 统一 axios 客户端与编排 API | `frontend/admin/src/api/*.ts` | P1-8 | 前端任务图页可见后端 AiTask | P0 || P4-4 | 补齐前端 vitest 基线 | `frontend/admin/src/tests/` | P4-3 | `npm test` 通过 | P1 || P4-5 | AionUi 补工作树 + 扩展安装 | `_external/aionui/`, `tools/aionui-extension-youding/` | P0 | `git -C _external/aionui checkout -f HEAD` 成功 | P1 || P4-6 | AionUi 端到端 ACP 联调 | `tools/youding_acp.py` | P4-5/P1-8 | 真实 `from-intent` 跑通 | P1 |### Phase 5 任务| 任务ID | 描述 | 文件/模块 | 依赖 | 验证 | 优先级 ||---|---|---|---|---|---|| P5-1 | 在执行器运行时注入 `sop_ref` | `backend/app/services/hermes/executors/{site_builder,content,deerflow,publish}.py` | P1 | DB `sop_refs` 有记录 | P1 || P5-2 | AgencyZH 角色加载器接入执行器 | `backend/app/services/hermes/agency/role_loader.py` | P1 | `accio`/`research` 运行时可读角色 | P1 || P5-3 | Skills 包在执行器内被调用 | `backend/app/services/registry/skill_pack_loader.py` | P1 | `skills` 表被查询 | P1 || P5-4 | ECC 默认关闭 + 按行业开启 | `_external/ecc/`, `backend/app/services/hermes/planner_service.py` | P5-1 | 无 ECC 时走默认、有行业配置时注入 SOP | P2 |### Phase 6 任务| 任务ID | 描述 | 文件/模块 | 依赖 | 验证 | 优先级 ||---|---|---|---|---|---|| P6-1 | `billing` executor 接入 TokenService | `backend/app/services/hermes/executors/billing_executor.py`, `backend/app/services/billing/token_service.py` | P1/P5 | Token 消耗写入 `token_ledger_entries` | P0 || P6-2 | `model_gateway` 记账落地 | `backend/app/services/model_gateway.py` | P1 | `model_call_ledger` 有记录 | P0 || P6-3 | `egress` executor 槽位计费 | `backend/app/services/hermes/executors/egress_executor.py`, `backend/app/services/egress_*.py` | P1 | 分配槽位后写入计费记录 | P1 || P6-4 | 余额不足拦截 | `backend/app/services/billing/wallet_guard.py` | P6-1 | 触发 `budget_exceeded` | P0 || P6-5 | L-Pro 门禁清单 | `docs/L-Pro-gate-checklist.md`（新建） | P6-4 | 清单全绿 | P1 |---## 8. 风险与缓释| 风险 | 影响 | 缓释措施 ||---|---|---|| **AionUi blobless 工作树未补齐** | ACP 扩展无法安装 | 在可直连网络执行 `git -C _external/aionui checkout -f HEAD`；如仍失败，先以 `git show HEAD:<path>` 读代码做协议层验证 || **WAF/UA 拦截** | 外部 API（TradeAI/GoodJob/平台）返回 403 | 所有出站 HTTP 使用 browser UA；`tools/smoke_orchestration.py` 已内置；新增平台适配时复用同一 UA 池 || **8000 端口冲突** | 后端启动失败 | 项目已使用 **8001**（`start-lan.bat` / `development.md`），继续沿用 8001；启动前 `tools/probe.py` 检查端口 || **218 空表导致业务链路跑空** | 执行器产出无业务上下文 | Phase 0 优先灌种子；`seed.py` 覆盖核心主数据（tenant/user/product/inquiry/order/logistics） || **TradeAI/GoodJob API Key 缺失** | 适配器无法真接 | P3-1 先配置环境变量；Key 未提供时保持诚实 failed（不静默成功），并给出配置指引 || **前端 0 测试** | UI 回归无门禁 | Phase 4 补 vitest 基线；每次前端改动跑 `npm run typecheck && npm run lint && npm test` || **12 引擎收敛回退** | 旧引擎绕过 Hermes 复发 | 已收口为 0（H 段 8/8），后续新增能力必须走 `ExecutorRegistry` + `hermes_task_bridge.ROUTERS` || **硬编码凭证 ~32 处** | 安全风险 | 跑 `tools/fake_scan.py` 定位；迁移到 `.env` 或 vault；禁止新代码硬编码 || **模型调用超时（180s→300s）** | 冒烟脚本误判 | `smoke_orchestration.py` 已设 `DISPATCH_TIMEOUT_FLOOR=300`；生产环境建议 Celery 任务单独设超时 || **双官网形态未收敛** | 3000 归属争议 | 已确认 B 路径：Vite 暖白 `#f5f4f0` 运行源；worktree Nuxt3 仅作开发迭代，不占用 3000 |---## 9. 验证与门禁（Gates）### 9.1 每阶段出口门禁| 阶段 | 门禁命令/脚本 | 通过标准 ||---|---|---|| Phase 0 | `python tools/probe.py` | PG/Redis/n8n/后端 health 全绿 || Phase 0 | `python backend/scripts/orchestration_selfcheck.py` | 19/19 PASS || Phase 0 | `python tools/db_probe.py --density` | 业务表非空≥20 || Phase 1 | `python tools/verify_executors.py` | 24/24 合规，能力无冲突 || Phase 1 | `python tools/smoke_orchestration.py --live --dispatch --approve --timeout 300` | ≥10 节点 done || Phase 1 | `python tools/capability_ledger.py` | 串联率 ≥40% || Phase 2 | `python tools/capability_ledger.py --platforms` | 平台串联率 ≥50% || Phase 2 | 抽查 `POST /api/v1/publish/distribute` | 真发或诚实 `not_configured` || Phase 3 | `python -m pytest tests/test_aeos_full_loop_dag.py -v` | 通过 || Phase 3 | `python tools/fake_scan.py` | HIGH=0 / MED=0 || Phase 4 | `cd frontend/admin && npm run typecheck && npm run lint && npm test` | 全绿 || Phase 4 | AionUi ACP 真实会话 | `initialize` + `prompt` + 任务 done || Phase 5 | 抽查 `evolution_task_records` / `sop_refs` | 有记录 || Phase 6 | `python -m pytest tests/unit/test_billing_executor.py` | 通过 || Phase 6 | 余额不足触发测试 | 返回 `budget_exceeded` || 全局 | `python tools/fake_scan.py` | HIGH=0 / MED=0 || 全局 | `python backend/scripts/orchestration_selfcheck.py` | 19/19 PASS |### 9.2 WAF-friendly 探测示例```bash# 所有外部探测使用 browser UAcurl -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0.0.0 Safari/537.36" \  -H "Accept: application/json" \  http://127.0.0.1:8001/api/v1/health```---## 10. 立即开始的下一步（Next 3 Actions）1. **双击 `start-lan.bat` 启动全套服务**，确认 `GET /api/v1/health` 200、PG/Redis/n8n 运行正常；若端口冲突，先跑 `python tools/probe.py` 定位。2. **跑数据基线**：在 worktree 内执行 `backend/.venv/Scripts/python.exe backend/seed.py` + `tools/seed_dev_users.py`，然后 `python tools/db_probe.py --density` 确认业务表有数据。3. **跑一次编排门禁基线**：`python backend/scripts/orchestration_selfcheck.py` + `python tools/smoke_orchestration.py --live --dispatch --approve --timeout 300`，确认当前 7/7 done 基线无回归，再进入 Phase 1 的 `media`/`seo`/`lead` executor 封装。---## 11. 文件/索引/记忆同步要求按项目规则「**产出即入索引**」，本文件产生后必须同步登记：1. **会话产出索引**：在 `docs/00-会话产出串联索引-2026-09-10.md` 新增一条 #54，记录本文件路径、一句话摘要、关键数字、下一步。2. **跨会话记忆**：在 `.workbuddy/memory/2026-09-11.md` 追加本方案的核心结论与下一步，供后续模型接手时快速续接。3. **Obsidian 同步**：由于 `docs/` 与 `.workbuddy/memory/` 已通过 NTFS Junction 直通 Obsidian Vault，写入即同步，无需额外操作。---## 12. 附录：关键文件速查| 文件 | 作用 ||---|---|| `AGENTS.md` | 硬锁与文档地图 || `docs/00-会话产出串联索引-2026-09-10.md` | 本轮全部产出索引 || `docs/未驱动串联资产盘点-2026-09-11.md` | 122 未串 / 49 孤儿页 / 42 桩平台 / 218 空表 || `docs/资产盘点审计-全维度-2026-09-11.md` | 112 维审计与风险 || `docs/审计复核确认-百维度范围与拓扑-2026-09-11.md` | 范围确认 + 拓扑图说明 || `docs/全链路串联设计-总纲-2026-09-10.md` | 架构 × 业务 18 步映射 || `docs/串联驱动设计-2026-09-10.md` | 五层架构 ↔ 代码落点 || `docs/编排主链打通实作记录-2026-09-10.md` | 代码交付与去假记录 || `docs/AionUi接入评估-2026-09-10.md` | ACP 桥设计说明 || `AEOS_GLOBAL_SYSTEM_REGISTRY.json` | 8 子系统机器注册表 || `docs/代码地图/topology.json` + `topology.md` | 依赖拓扑 || `development.md` | 开发命令与回归清单 |---*本方案由 software-architect（Bob）基于 2026-09-11 最新审计与源码实测编制，所有数字可追溯到 `tools/*` 可重跑脚本。*---# 中篇 · 组装总方案复核补遗（勘误与技术指导）---## 一、勘误表（以实测代码为准）### E1 · Alembic 单 head = 109 ✅（方案正确，旧记忆口径作废）- 实测链：`105 ← 104`，`107 ← c6b6225da363`，`108 ← 107`，**`109_rls_batch2_pilot_reapply ← 108`**，且全库无任何迁移 down 指向 109。- **结论**：单 head = `109_rls_batch2_pilot_reapply`，方案 §3 ENV-LOCK-01 写法正确。- ⚠ 记忆中"单 head 105"为 09-10 旧口径，**作废**，以 109 为准。### E2 · P1-7「补 orders 路由」描述错误 → 改为「打通 orders 前后端最后一公里」- 方案原文说 orders 路由缺失（`routes/orders.py` 不存在）。实测：  - **后端路由已存在**：`backend/app/api/v1/orders.py`（v1 顶层散落文件，`auto_discovery` 机制扫描 v1 顶层 `.py` 自动挂载，无需 `routes/orders.py`）；  - **order_executor 已存在且已注册**（`executors/order_executor.py`，`__init__.py` 已 import）；  - **前端已有 3 处调用 `/api/v1/orders`**：`useQueueBadges.ts`、`views/client/queues/fulfillment.vue`、`views/logistics/orders-track.vue`；  - **真正缺失**：`frontend/admin/src/api/orders.ts` 客户端封装不存在（3 处调用是散写 URL）。- **订正后任务**：P1-7 改为 ① 抽取 `api/orders.ts` 统一封装；② 三处散写调用改为走统一客户端；③ 用 `orchestration_selfcheck` 实测 orders 路由 401（已挂载判定）。### E3 · Phase 6 门禁引用了不存在的测试文件（先建后跑）- 方案 §9.1 写 `python -m pytest tests/unit/test_billing_executor.py`。实测 `backend/tests/unit/`（34 个文件）**无任何 billing/wallet/egress 测试**。- **订正**：该门禁性质是"新建后跑"——P6-1 交付时必须**同步新建** `tests/unit/test_billing_executor.py`（覆盖：正常扣费写入 `token_ledger_entries` / 余额不足返回 `budget_exceeded` / mock 模式诚实降级三用例），门禁才可执行。同理 §9.1 各门禁在执行前先 `ls` 确认脚本存在。### E4 · 工具脚本运行目录混淆（高风险，容易跑错位置）方案所有验证命令统一写 `python tools/xxx.py`，但实测工具脚本**不在 worktree 内**：| 脚本 | 实际位置 | 运行目录 ||---|---|---|| `verify_executors.py` / `capability_ledger.py` / `smoke_orchestration.py` / `db_probe.py` / `fake_scan.py` / `seed_dev_users.py` / `probe.py` / `code_atlas_gen.py` / `build_topology.py` / `youding_acp.py` | **工作区根 `tools/`** | `C:\Users\Administrator\Documents\上线网站开发完成` || `orchestration_selfcheck.py` | **worktree `backend/scripts/`** | worktree 根 || `seed.py` | **worktree `backend/seed.py`** | worktree 根（用 `backend/.venv/Scripts/python.exe`） |- **订正**：执行方案任务时严格按下表切目录；在 worktree 根跑 `python tools/verify_executors.py` 会直接报 `No such file`，反过来在工作区根跑 `backend/scripts/orchestration_selfcheck.py` 也会报错。建议给 `tools/` 脚本统一加 `--worktree` 参数或在文档头部注明期望 cwd。---## 二、P1-9 persona_ref 悬空 · 代码级修复指导（实测定位）**问题确认（非猜测，已实测）**：- 消费端：`backend/app/services/tasks/hermes_task_bridge.py:462` → `node = TaskNode(..., persona_ref=data.get("persona_ref"))`；- 定义端：`backend/app/schemas/hermes_orchestration.py:27` 的 `TaskNode` **没有 `persona_ref` 字段**，且类未配置 `model_config = ConfigDict(extra=...)`；- Pydantic v2 默认 `extra='ignore'`：构造时不报错，但 **persona_ref 被静默丢弃** → 下游执行器永远拿不到 persona。这正是"悬空"的机理，且属于**静默数据丢失**（比崩溃更隐蔽，违反"不假交付"精神）。**修复三步（Phase 1 P1-9 执行）**：```python# ① schemas/hermes_orchestration.py — TaskNode 增加字段（sop_ref 之后）：persona_ref: Optional[str] = Field(None, description="AgencyZH persona role id, resolved by role_loader at runtime")# ② planner_service.py — 拆解时按意图产出 persona_ref（至少给 accio/research 节点赋默认角色），#    模板场景（templates/outreach_scenario.py 等）中补 persona_ref 键。# ③ 执行器侧（base.py 或 role_loader.py）— ExecutorContext 增加 persona 解析钩子：#    context.resolve_persona(node.persona_ref) -> PersonaDefinition | None#    persona_ref 为 None 或查不到时返回 None 并记录日志，禁止编造默认 persona（不假交付）。```**验证**：构造带 `persona_ref="role:trade_writer"` 的 TaskNode → 断言 `node.persona_ref` 不为 None；查不到的 ref 断言执行器日志出现 `persona not found, degraded` 而非假成功。---## 三、补强 1 · TradeAI 许可证风险（方案 §8 未列，商用前必须处理）实测 `backend/app/services/adapters/tradeai/README.md` 明确记载：> Trade AI Agent 来源 Gitee `LBones-li/agent_trade_b`，README 声明 MIT 但**根目录无 LICENSE 文件**——商用合入前必须向作者书面确认（总纲 §9.4-1），未完成前不得对外分发含本适配器的版本。**处置**：1. §8 风险表增加一行：`TradeAI 适配器许可证未确认 | 商用分发法律风险 | P3-2 动工前向作者取得书面授权或保留出处注释 + 律师复核；未确认前该适配器版本锁内测`。2. P3-2 增加前置条件：授权确认截图归档到 `00-文档中心/审计/`。## 四、补强 2 · "丝滑流畅"专项（系统体验保障指导）装配期最容易把系统做"卡"，以下作为各阶段横向约束（并入每个 Phase 出口自检）：| 层 | 约束 | 落点 ||---|---|---|| DB | PG@5433 连接池上限与 Celery worker 并发匹配（worker 数 × 并发 ≤ pool 上限），防止连接耗尽全站转圈；229 表批量 seed 用分批 commit（每 500 行），避免长事务锁表 | `config/dev/.env`、`seed.py` || 编排 | 24 执行器并发默认 `max_parallel: 3`（GraphPolicies 已有）；单节点 `TaskBudget` 必填上限，防一个平台桩死循环拖垮整图；失败节点走 `on_fail: degrade/skip` 而非 abort 全图 | `schemas/hermes_orchestration.py` || 前端 | 49 孤儿页并入路由时**一律路由级懒加载**（`() => import(...)`），防止主包膨胀首屏变慢；orders-track 等轮询页轮询间隔 ≥10s 且页面隐藏时暂停 | `router/index.ts` || 出站 | 42 平台接线必须串行 tier 分批放量，浏览器 runtime 复用 profile 会话，禁止每任务冷启动一个浏览器（内存爆） | `tier_router.py`、`browser_executor.py` || 可观测 | 全链路 trace_id 已打通——每个 Phase 收尾抽查 1 条真实任务的 trace 链路完整性，作为"丝滑"的量化证据 | `AiTask.output_json` |## 五、补强 3 · 门禁存在性预检脚本（防跑错/防引用不存在的脚本）E3/E4 的根因是"门禁引用未先验存在"。给出一次性预检命令（在**工作区根**执行）：```bash# 逐阶段门禁前置检查：存在才跑，缺失则标记 BUILD-FIRSTfor f in tools/verify_executors.py tools/smoke_orchestration.py tools/capability_ledger.py \         tools/db_probe.py tools/fake_scan.py tools/probe.py; do  [ -f "$f" ] && echo "OK    $f" || echo "MISS  $f"donecd 上线网站.worktrees/agents-install-vscode-cline-deploy-strix && \  { [ -f backend/scripts/orchestration_selfcheck.py ] && echo "OK    orchestration_selfcheck" ; \    [ -f backend/seed.py ] && echo "OK    seed.py" ; }```建议把该清单写进 `development.md` 回归清单节，作为每个 Phase 门禁的第 0 步。---## 六、订正对执行顺序的影响- **P0–P6 阶段划分、关键路径、依赖关系：全部维持不变**（勘误均为任务内容级，未动摇骨架）。- P1-7 内容改写（E2）后，其验证从"orders 表可写"升级为"前端统一客户端 + 后端 401 挂载判定 + orders 表可写"三项。- P3-2 增加许可证前置条件（补强 1），**P3 整体不阻塞**：P3-1/P3-3/P3-4/P3-5/P3-6 可先行，仅 `adapters/tradeai` 代码合入对外分发受限。- 下一步三件事（方案 §10）维持不变，即刻可执行。---*本补遗由主理人复核要求触发，全部结论基于 2026-09-11 实测代码树（文件级证据），与总方案配合阅读。*---# 下篇 · Obsidian 双向直通同步架构设计与回顾订正---## 一、同步架构设计（权威版）### 1.1 物理拓扑（实测认证 2026-09-11）```C:\Users\Administrator\Documents\├── 上线网站开发完成\                        ← 工作区（物理实体所在地）│   ├── docs\                    [实体目录]  ──被──→ Vault\01-权威文档与系统全景 (Junction)│   ├── 00-文档中心\             [实体目录]  ──被──→ Vault\03-历史方案与文档中心 (Junction)│   ├── .workbuddy\memory\       [实体目录]  ──被──→ Vault\02-AI长期记忆与每日日志 (Junction)│   └── my-obsidian-notes\       [Junction]  ──指向──→ Documents\Obsidian Vault（反向直通，读 Vault 全库用）│└── Obsidian Vault\优丁YouDing-AI智能大脑\    ← Obsidian 呈现层（4 个 Junction 入口）    ├── 01-权威文档与系统全景   = Junction → 上线网站开发完成\docs          （153 文件）    ├── 02-AI长期记忆与每日日志 = Junction → 上线网站开发完成\.workbuddy\memory（19 文件）    ├── 03-历史方案与文档中心   = Junction → 上线网站开发完成\00-文档中心    （88 文件）    └── 04~07 + 散篇 *.md                    （Vault 原生文件，非直通区）```### 1.2 设计原则（为什么这样建）| 原则 | 说明 ||---|---|| **单实体多入口** | 每份文档物理上只存在一份（工作区），Vault 侧全是 NTFS Junction（reparse point）指回来。**不存在"同步"这个动作**——因为是同一个文件，天然零延迟、零冲突、零丢失。 || **实体在工具侧，不在 Vault 侧** | 工作区是 AI/IDE 的读写主场；Vault 只是 Obsidian 的展示窗口。删除工作区文件 = Vault 同名消失（无回收缓冲），这是特性不是缺陷：保证两边永远一致。 || **方向明确防误删** | `my-obsidian-notes`（工作区侧 Junction → Vault 根）是唯一反向入口，用于 AI 直接读 Vault 原生文件（04~07 速查、散篇记忆）。Vault 侧 3 个 Junction **不可反向重建**成实体，否则出现双实体分叉。 || **写哪边都一样** | 通过任一路径写入都落在同一物理文件。但**规约上统一从工作区侧写入**（路径稳定、工具链齐全），Vault 侧只读。 |### 1.3 四个镜像区的分工| 区 | 实体 | 内容 | 容量（今日实测） ||---|---|---|---|| **01-权威文档与系统全景** | `docs/` | 技术方案、总纲、审计、代码地图、本体、台账——**当前事实的真相源** | 153 文件（今日 39 / 历史 114） || **02-AI长期记忆与每日日志** | `.workbuddy/memory/` | 日期化工作日志 + MEMORY.md——**跨会话续接的记忆层** | 19 文件 || **03-历史方案与文档中心** | `00-文档中心/` | 2026-06~08 的归档抢救、审计 HTML、交付报告——**历史沉积层，只读参考** | 88 文件（今日 0，全部历史） || **04~07 散篇（非直通）** | Vault 原生 | UI 规范速查、组件契约、8 大子系统字典、7 步闭环速查、每日记忆全量存盘 | 5 文件 |### 1.4 维护规约（对一切 AI/IDE 生效）1. **产出即入索引，索引即入 Vault**：任何新文件写进 `docs/` 瞬间自动进 01 区，无需任何同步操作；但必须在 `docs/00-会话产出串联索引-*.md` 登记（硬规矩），否则 Vault 里能看到文件却没有脉络。2. **订正历史文档用"头部插订正块"，不改正文**：历史快照文档（如 09-08/09-10 实测记录）的旧数字是**当时真实**的，就地改写会破坏时间线。订正方式 = 在过时结论处插入 `> ⚠ YYYY-MM-DD 订正：…` 引用块并指向新文档。权威口径类文档（AGENTS.md、强制索引、注册表 JSON）例外，直接改正文并在行内标注订正日期。3. **只读区不写**：03 区（00-文档中心）是历史沉积，AI 不得往里写新文件；除非用户明确要求归档。4. **禁止在 Vault 侧手工重建目录结构**：4 个 Junction 之外不要新建映射目录（会出现实体分叉）；要加新区就在工作区建实体、再 `mklink /J` 挂到 Vault。5. **Junction 健康检查**（一次性命令，纳入日常巡检）：```python# 用 Win32 API 判 reparse，避免 cmd 输出编码坑import ctypes, osREPARSE = 0x400GetFileAttributesW = ctypes.windll.kernel32.GetFileAttributesWpairs = [  # (Vault侧Junction, 工作区侧实体)    (r"...\01-权威文档与系统全景", r"...\docs"),    (r"...\02-AI长期记忆与每日日志", r"...\ .workbuddy\memory"),    (r"...\03-历史方案与文档中心", r"...\00-文档中心"),]for j, real in pairs:    assert GetFileAttributesW(j) & REPARSE, f"Junction 失效: {j}"    assert os.path.samestat(os.stat(j), os.stat(real)), f"指向漂移: {j}"```---## 二、全量回顾结论（2026-09-11，历史 218 文件）### 2.1 覆盖范围与方法- **跳过**：今日（09-11）同步的 39 个文件（01 区）+ 3 个（02 区）——均为本会话产出，内容已在掌握中。- **回顾对象**：01 区历史 114 个 + 02 区历史 16 个 + 03 区历史 88 个 = **218 个历史文件**。- **方法**：3 份历史总纲精读章节结构；9 份 09-09/09-10 承重文档抓关键数字断言（head/表数/串联率/执行器数/mock 判定）交叉核对；与今日代码树实测（执行器 24 个、head=109、orders 路由在位、TradeAI/GoodJob 真桥代码在位）对照。- **03 区 88 个全部为 06-08 月归档**（审计 HTML/交付报告/归档抢救），内容已过时但性质就是历史存档，无需订正，保留原样。### 2.2 发现的问题与处置（6 处全部订正完毕）| # | 问题 | 位置 | 处置 ||---|---|---|---|| C1 | **head=105 过时**（现代码树=109） | `后端运行环境强制索引-必读.md`、`数据库实测快照-2026-09-10.md`、`多模型设计统一裁定书` F6、`AEOS_GLOBAL_SYSTEM_REGISTRY.json`、工作区 AGENTS.md | 强制索引/裁定书/快照：行内或头部插订正块指向补遗 E1；**AGENTS.md 与注册表 JSON 直接改正文**（JSON 已验合法） || C2 | **执行器数"9 个"过时**（实测 24 个） | `后端运行环境强制索引-必读.md` §执行器 | 标题改为"4→9→24"，原表降级为"09-10 时点快照"，以 `verify_executors.py` 实跑为准 || C3 | **aionui"孤立"残留** | 强制索引 §未驱动盘点摘要 | 摘要句改"孤立项目 0"，补 ACP 桥订正说明 + 串联率双口径（15.3%→17.3%） || C4 | **表数口径混用**（229 vs 239 vs 237~255） | 强制索引端口表 | 补口径注释：库实测 229 是基准，ORM 声明 237~255 是另一口径，禁止混用 || C5 | **总方案自身错误**：TradeAI/GoodJob 写"当前 mock" | `优丁全项目串联组装总方案` §1.3/§4.2 | 读执行器代码终裁：**代码级 mock 已于 09-10 移除、真桥已接**，缺的是 `TRADEAI_BASE_URL`/`GOODJOB_BASE_URL` 配置与端到端联调 → 状态从 🔴"当前 mock"改 🟠"真桥已接·运行时未实测"，Phase 3 目标从"替换 mock"改"真桥端到端跑通" || C6 | **裁定书 T0 条款含过时值**（F6 head=105 会误杀正确设计） | `多模型设计统一裁定书` | 新增 F6※ 订正行：T0 判定时 head 以 109 为准 |### 2.3 判定为"健康、无需处理"的部分- **Junction 本体**：3 个映射全部 reparse=True、same-inode 一致，方向正确（实体在 WS），无一失效。- **`全链路串联设计-总纲`/`串联驱动设计`/`系统组装总方案-09-09`**：架构原则与今日方案同源（三层架构/台账驱动/先数据后接线），无冲突，今日总方案是其升级版，保留即可。- **`编排内核设计对照与校正`/`多模型设计统一裁定书` 的三级判定链**：方法论仍有效，仅 F6 数字订正（见 C6）。- **02 区 16 个历史日志**：日期化日志性质即"当日快照"，其中的旧数字不构成误导（已由 MEMORY.md 承担长期口径），全部保留原样。- **03 区 88 个归档**：历史沉积层，不动。### 2.4 本次设计的"防再犯"机制本次 6 处问题的共同根因：**快照文档被当成"活文档"引用**。机制上落三条：1. **快照文档头部必须写"生成时点 + 引用前重跑探针"**（`数据库实测快照` 已具备，其他快照类补齐）。2. **T0 事实层的数字必须可追溯到"实跑命令 + 时点"**，裁定书 F6※ 已示范。3. **跨文档同口径数字建立"订正一处、同步 N 处"清单**——本次 head=105 波及 5 处文件，已在补遗 E1 记录完整波及面。---*本设计文件由主理人"回顾+纠正+仔细设计"指令触发；全部结论基于 2026-09-11 实测（ctypes API 验证 Junction、执行器目录实数、迁移链全量比对）。Vault 侧经 Junction 即时可见本文件。*---*合订版说明：三册原件为《优丁全项目串联组装总方案-2026-09-11.md》《组装总方案复核补遗-2026-09-11.md》《Obsidian同步架构设计与回顾订正-2026-09-11.md》，内容与本合订版逐字一致（除头部与跨册引用措辞）。后续修订以本合订版为唯一工作版本。*> **📌 2026-09-12 起本合订版已被《优丁全项目串联组装总案-修订版v2-2026-09-12.md》取代（14 处纠错 + 前沿技术评估）——后续修订以 v2 为唯一工作版本，本册为历史存档。**