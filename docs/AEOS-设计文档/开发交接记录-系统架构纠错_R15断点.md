# 开发交接记录 —— 系统架构纠错与设计补充（Qoder CN 会话断点续做）

> 生成时间：2026-09-02 10:30  
> 断点来源：Qoder CN IDE 会话 **系统架构纠错与设计补充**（Quest task-id: `task-f979544a09d54ea891c4`，状态 `Error`）  
> 聊天记录存储：`%APPDATA%\QoderCN\User\globalStorage\state.vscdb`  
> 本记录用途：替代未完成的会话交接，让下一个 Agent 直接从 **轮15** 继续开发。

---

## 一、断点 snapshot

Qoder 在该会话中最后一条发出的消息是：

> **“编排信号充分，攻击模式分类明确。现在落地轮15：先建注入语料库与 N5 净化层：”**

随后会话中断，**没有来得及写交接记录**。

当前代码库（真实工作目录）：

```
C:\Users\Administrator.WIN-36O2UQRI3U1\Desktop\上线网站开发完成\上线网站.worktrees\agents-install-vscode-cline-deploy-strix\backend\
```

---

## 二、会话整体目标（不要跑偏）

本会话从 `docs/archive/GPT文档纠错与实现设计补充_V1.0.md` 出发，对 GPT 设计文档做“对齐式改造”而不是从零重建。

核心设计冻结点：
- 本系统 = **Control Plane**（现有 FastAPI 单体）。
- Hermes / DeerFlow / n8n / Harness / ModelGateway / BrowserRuntime = **Execution Plane Adapter**。
- 统一任务状态机以 `deerflow/state_machine.py` 为基线扩展。
- 计费/钱包/CRM/Stripe **已存在，禁止重建**，只补 MeterEvent 埋点。

---

## 三、轮15 之前已经做了什么

| 文件 | 路径 | 当前状态 | 说明 |
|---|---|---|---|
| 设计基线报告 | `docs/archive/GPT文档纠错与实现设计补充_V1.0.md` | ✅ 已归档 | 完整纠错清单、差距矩阵、P0-P5 阶段路线 |
| 注入语料库 | `backend/app/services/wangcai/security_corpus.py` | 🗑 **已清理**（2026-09-02） | 轮15 唯一"写到一半"的孤立产物：创建后 **0 引用 / 0 消费**；样本实际 **33 条**（非原记 56）；其 substring 匹配范式与总定稿 **R9（结构化隔离+不扩权）** 不一致 → 已删除，备份 `fix-backup-20260902-cleanup/security_corpus.py.bak` |
| Pipeline 核心迁移 | `backend/alembic_migrations/versions/090_pipeline_core.py` | ✅ 已创建 | 建 `pipeline_runs` / `pipeline_steps`，11 态， tenant 隔离 + lease + idempotency |
| Pipeline 证据迁移 | `backend/alembic_migrations/versions/091_pipeline_evidence.py` | ✅ 已创建 | 建 `step_evidence` / `review_tasks`，证据类型 + 人工复核三态 |
| Pipeline 状态机 | `backend/app/services/pipeline/state_machine.py` | ✅ 已创建 | 11 态转移表、有界重试 ≤3、非法转移抛异常 |
| Pipeline 关卡接线 | `backend/app/services/pipeline/wiring.py` | ✅ 已创建 | CleanseGate 注册 `brand_guard` / `compliance`；ReviewGate 注册 `content_scorer` / `eeat` |
| 旺财 Router N5 挂点 | `backend/app/services/wangcai/router.py` | ✅ 已预留 | `_default_sanitizer()` 默认挂 `hermes/brand_guard.sanitize_public_copy`（第 303 行） |
| DeerFlow 状态机 | `backend/app/services/deerflow/state_machine.py` | ⚠️ 已存在/可能调整 | 旧 DeerFlow 9 态状态机，需与本 Pipeline 11 态区分清楚 |

> 会话中断前 Qoder 提示“42 个文件已变更 +4605 -10”，上表只列出与轮15 直接相关的文件；其余多为 docstring/测试/修复类改动，见 `docs/archive/历史记录-2026-08-30/` 各轮报告。

---

## 四、轮15 断点处：偏差与清理（2026-09-02 更新）

轮15 会话最后一条消息是"先建注入语料库与 N5 净化层"。经对照代码库实测 + 总定稿架构（`AI_Global_Growth_OS_统一架构与整合总纲_V1.0.md`）复核，结论如下：

### 偏差定位
- **偏差条目**：本记录原 §三 标注 `security_corpus.py` 为"✅已创建 / 56 条样本"，与实测不符。
- **实测事实**：该文件实际 **33 条**样本（越狱10 / 覆盖10 / 套取10 / 组合3）；且在整个 `backend/app` 源码中 **0 引用、0 消费**，是轮15 中**唯一一个"写到一半"的孤立产物**。

### 与后续轮次流程一致性裁决（对照总定稿）
- 总定稿 **R9 红线（§7 / §7.7）**：消息类外部输入一律视为不可信，标准是 **结构化隔离 + 工具权限不因消息扩大**。
- 总定稿 **§7A.2 阶段2**：旺财访客输入=不可信（R9），生成回复**必须过 `hermes/brand_guard.sanitize_public_copy` 清洗**。
- 总定稿 **§6.4 清洗关卡**：`CleanseGate` 聚合 `brand_guard` + `compliance_scanner` + 敏感词/事实断言/查重——**不含"语料库 substring 匹配"项**。
- 裁决：`security_corpus.py` 的"固定样本 substring 匹配"范式 **不符合**后续轮次处理流程，且完全孤立 → **清理（删除）**，不补全。

### 清理动作（已执行 2026-09-02）
- 删除 `backend/app/services/wangcai/security_corpus.py`。
- 备份至 `fix-backup-20260902-cleanup/security_corpus.py.bak`（可恢复）。
- `router.py::_default_sanitizer()` 仅挂 `brand_guard`（符合 §7A.2），**未改动**，保持通畅运行。
- 清理后，轮15 注入防御由现有 `brand_guard` + 总定稿 R9 范式覆盖，无需独立语料库 checker。

### 剩余未做（清理后真实断点）
1. Pipeline 关卡（`CleanseGate`）暂未注册注入类 checker —— 按总定稿 §6.4 该职责已由 `brand_guard`+`compliance` 承担，非必需。
2. 迁移文件 `090/091` 尚未在开发库实际执行验证。
3. 尚无回归测试（删除语料库后，原"56 条拦截"验收目标取消）。

---

## 五、续做顺序（偏差清理后，按总定稿后续轮次）

> 轮15 的"注入语料库 + N5 净化层"已判定为孤立产物且范式不符总定稿 R9，已于 2026-09-02 清理。下文不再重建语料库 checker，直接进入与总定稿一致的后续轮次。

### Step 1 —— 执行并验证迁移 090/091（补齐轮15 残留）

在 PG+Redis 环境（或 SQLite 开发兜底）执行：

```bash
cd backend
python -m alembic upgrade +2
```

确认：
- `pipeline_runs` / `pipeline_steps` / `step_evidence` / `review_tasks` 四表已建。
- SQLite 下 `CHECK` 约束和索引不报错（迁移里已做 `_table_exists` 与方言兼容）。

### Step 2 —— 补 Pipeline 回归测试（不依赖语料库）

新增测试文件：

```
backend/tests/pipeline/test_cleanse_gate.py
backend/tests/wangcai/test_router_sanitize.py
```

断言：
- `brand_guard` + `compliance` 在 `CleanseGate` 中正确串联注册。
- 正常产品咨询（如"岩棉板密度是多少"）不触发 error。
- `CleanseGate` 整体运行后，含占位符/违禁词的样本 `passed=False`。

### Step 3 —— 轮16+：按总定稿 §8 迁移总表 / §6.6 推进

| 轮次 | 内容（对照总定稿） |
|---|---|
| 轮16 | Model Gateway 重构（`app/services/model_gateway/`，总定稿 §8 / §6.6 P1） |
| 轮17 | Skill/MCP/Plugin 注册表持久化（迁移 083） |
| 轮18 | Policy Engine + Credential Vault（§1.2 / §6.6 P2） |
| 轮19 | Trace / Experience / Evolution 接线（§6.6 P3） |
| 轮20 | MeterEvent 埋点 + 财务对账（§6.6） |

---

## 五（续）、轮16 Model Gateway 重构 —— 已完成（2026-09-02）

> 按用户"继续按顺序进行开发"指令，轮16 已落地。设计对齐总定稿 §4.6 / §8 迁移总表 084 / 038 Model Router。

### 设计裁决（对齐总定稿，避免重建）
- 总定稿 §4.6：Model Gateway = `app/services/model_gateway/`（router/ledger）；能力标签 `reasoning/coding/vision/writing/translation/structured_output/cheap/fast`；业务只传 `required_capabilities + budget`；每调用写 `model_call_ledger` → 汇总 `token_ledger`；保留 `MVP_LAUNCH` mock 门。
- 现有 `ai_engine.get_ai_engine()` 已被 `wangcai/llm.py` 视为"Model Gateway 前置纪律"（docstring 明示）。**因此本轮不重建 ai_engine，而是新建 `model_gateway/` 门面复用 `get_ai_engine()`**，符合"加新模块而非改旧文件 + 不重建已存在模块"。

### 交付物清单
| 文件 | 状态 | 说明 |
|---|---|---|
| `backend/app/services/model_gateway/__init__.py` | ✅ 新建 | 包导出 + `is_gateway_enabled()` 特性开关读取 |
| `backend/app/services/model_gateway/capability.py` | ✅ 新建 | 八类能力标签常量 + `CAPABILITY_TO_TIER` 映射 + `normalize()` |
| `backend/app/services/model_gateway/router.py` | ✅ 新建 | `ModelRouter.resolve_tier()`：能力+budget → ai_engine 场景 tier（预算紧张优先 cheap；038 评分维度并入） |
| `backend/app/services/model_gateway/ledger.py` | ✅ 新建 | `CostLedger`：`model_call_ledger` 最佳努力写入（async+to_thread，异常吞掉不影响主链路）；`aggregate_to_token_ledger()` 幂等汇总至 `token_ledger_entries` |
| `backend/app/services/model_gateway/gateway.py` | ✅ 新建 | `ModelGateway.generate()` 门面：复用 `get_ai_engine()`，签名兼容 `ai_engine.generate()`，调用后记账 |
| `backend/alembic_migrations/versions/095_model_gateway_ledger.py` | ✅ 新建 | `down_revision='094'`；建 `model_capabilities` + `model_call_ledger`（tenant 隔离、CHECK 约束、方言兼容、PG COMMENT） |
| `backend/app/core/config.py` | ✅ 改 | 新增 `MODEL_GATEWAY_ENABLED: bool = False`（默认关，零回归） |
| `backend/app/services/wangcai/llm.py` | ✅ 改 | `WangcaiLLM.answer()` 经 `MODEL_GATEWAY_ENABLED` 开关接入网关（默认关）；启用后传 `required_capabilities=["writing","translation","cheap"]` |

### 验证（已执行）
- 8 个新增/改动文件 `py_compile` 全部通过。
- 运行时冒烟（shim 包，避开重的 `app.services.__init__`）：`resolve_tier` 各能力映射 + budget 收紧、`normalize` 过滤、`build_entry` 序列化、`get_model_gateway()` 单例、`record()` 缺 DB 安全 no-op —— **全部通过**。
- 默认开关 `MODEL_GATEWAY_ENABLED=False`：旺财 N4 仍走原 `engine.generate()`，**零行为变化、通畅运行**；启用后仅旁路写入 `model_call_ledger`，记账失败不阻断主链路。

### 下一步（轮17 起）
- `python -m alembic upgrade head` 执行 095（环境可用时）→ 验证 `model_capabilities`/`model_call_ledger` 建表。
- 轮17：Skill/MCP/Plugin 注册表持久化（迁移 083 已建表，需落注册逻辑）。

---

## 六、红线与注意事项

1. **不要重建已存在模块**：Hermes/DeerFlow/n8n/Evolution/计费/钱包已存在，只做 Adapter/Protocol 包装。
2. **不要硬依赖 DeepSeek Harness**：仍是 developer preview，接口+桩即可。
3. **SQLite 模式 Celery 不可用**：异步任务验收必须切到 PG+Redis 环境。
4. **N5 净化层不要重复造轮子**：现有 `app/core/security/claw_patrol.py` 已有 Prompt 注入正则；注入防御统一走总定稿 **R9（结构化隔离 + 不扩权）+ `brand_guard` 清洗 + `claw_patrol` 正则**，不再维护独立语料库（原 `security_corpus.py` 已清理，2026-09-02）。
5. **终态不可再转移**：Pipeline 状态机已定义终态 `done` / `failed`，后续代码必须遵守。

---

## 七、关键文件速查

| 用途 | 绝对路径 |
|---|---|
| 设计基线 | `C:\Users\Administrator.WIN-36O2UQRI3U1\Desktop\上线网站开发完成\docs\archive\GPT文档纠错与实现设计补充_V1.0.md` |
| 语料库（已清理，仅存备份） | `C:\Users\Administrator.WIN-36O2UQRI3U1\Desktop\上线网站开发完成\fix-backup-20260902-cleanup\security_corpus.py.bak` |
| Pipeline 清洗/复核 | `...backend\app\services\pipeline\gates\cleanse.py` |
| Pipeline 关卡接线 | `...backend\app\services\pipeline\wiring.py` |
| Pipeline 状态机 | `...backend\app\services\pipeline\state_machine.py` |
| 旺财 Router N5 挂点 | `...backend\app\services\wangcai\router.py`（第 303 行 `_default_sanitizer`） |
| 现有安全防火墙 | `...backend\app\core\security\claw_patrol.py` |
| 迁移 090 | `...backend\alembic_migrations\versions\090_pipeline_core.py` |
| 迁移 091 | `...backend\alembic_migrations\versions\091_pipeline_evidence.py` |
| **轮16 Model Gateway 门面** | `...backend\app\services\model_gateway\`（capability/router/ledger/gateway/__init__） |
| 轮16 成本记账迁移 | `...backend\alembic_migrations\versions\095_model_gateway_ledger.py` |
| 轮16 特性开关 | `...backend\app\core\config.py`（`MODEL_GATEWAY_ENABLED`） |
| 轮16 旺财接入点 | `...backend\app\services\wangcai\llm.py`（`WangcaiLLM.answer()`，开关默认关） |
| **轮16 交付物归档（总定稿副本）** | `C:\Users\Administrator.WIN-36O2UQRI3U1\Desktop\总定稿开发文件不在扩张\轮16_Model_Gateway交付物\`（含本记录副本 + model_gateway 包 + 迁移 095 + 改动记录） |
| 顺势开发导航索引（三源合一） | `C:\Users\Administrator.WIN-36O2UQRI3U1\Desktop\总定稿开发文件不在扩张\顺势开发导航索引.md`（本记录 + 统一架构总纲 + Qoder CN 聊天记录 的中央索引） |

---

## 八、一句话总结

**断点状态（2026-09-02 更新）**：轮15 会话在"先建注入语料库与 N5 净化层"处中断。经核查，`security_corpus.py` 为唯一"写到一半"的孤立产物（0 引用、范式不符总定稿 R9），**已清理**（备份 `fix-backup-20260902-cleanup/`）。注入防御改由现有 `brand_guard` + R9 范式承担。剩余真实断点：迁移 090/091 未验证、缺回归测试。续做按总定稿后续轮次推进——**轮16 Model Gateway 重构已完成**（新建 `app/services/model_gateway/` 能力路由+成本记账门面，复用 `get_ai_engine()`，新增迁移 095，特性开关默认关零回归），见 §五（续）。轮17 起进入 Skill/MCP/Plugin 注册表持久化。

---

## 九、交付物归档位置（2026-09-02）

轮16 全部交付物已**复制归档**至总定稿目录（源 worktree 文件保留，未删除，可恢复优先）：

```
C:\Users\Administrator.WIN-36O2UQRI3U1\Desktop\总定稿开发文件不在扩张\轮16_Model_Gateway交付物\
├─ 交付清单.md                          ← 交付物总索引（结构/明细/验证/合入步骤）
├─ 开发交接记录-系统架构纠错_R15断点.md   ← 本文件副本
├─ model_gateway\                      ← 新建门面包（5 文件）
├─ migration\095_model_gateway_ledger.py
└─ modified\改动记录.md                 ← 既有文件最小化 diff（config.py / llm.py）
```

接收方在目标 backend 合入步骤见 `交付清单.md` 第四节；迁移 `095` 的 `down_revision='094'` 需按目标库实际迁移链 head 修订。
