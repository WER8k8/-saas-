# P4 前端去向裁定决策（T4 · 2026-09-12）

> **性质**：T4 收口决策 · 固化"双真相源分层"口径，消除 8 件套/AGENTS.md 里"官网唯一技术栈"的撒谎式表述。
> **裁定依据**：`docs/8件套纠错审计与收敛设计-2026-09-08.md`（默认 A）＋ `docs/多模型设计统一裁定书` F1/F2 + T4 物理取证。
> **B1 口径**：A 门禁全绿 + 可复跑脚本/物理取证为唯一真值基准。

---

## 1. 物理取证（2026-09-12 实跑 LS，防旧结论当现状）

| 真源 | 路径 | 形态 | 取证 |
|---|---|---|---|
| 开发真相源 | worktree `frontend/`（Nuxt） | Nuxt 开发版：`nuxt.config.ts`/`middleware/`(geo-routing/tenant)/`composables/`(useSEOHead·useGEOContent)/`app.vue`/`layouts/`/`locales`(12 语) | ✅ 存在 |
| 运行真相源 | 主要备份 `frontend/` | Vite+AntDV 运行版：`admin/`(src/main.ts+vite)+`3000/`、`127.0.0.1/` 两个**带 `.nuxt` 构建产物**的运行实例（Nuxt dev 镜像） | ✅ 存在 |
| 运行源 git 状态 | 主要备份 `frontend/` | git 无提交 = 运行副本 | 改前须回同步源 |

## 2. 裁定结论（双真相源分层 · 方案 A 默认）

**谈什么，以哪个为准**——这是唯一稳定口径：

| 话题 | 真相源 | 说明 |
|---|---|---|
| 代码结构 / 组件 / API 客户端 / 路由 | **worktree `frontend/`（Nuxt 开发版）** | 持续开发发生地，含最新 composables/middleware |
| 端口 3000 / 对外官网 / 主题色 / 启动命令 | **主要备份 `frontend/`（Vite 运行版）** | 当前 3000 实跑源，git 无提交=运行副本 |
| 主色 | **分层**：平台/后台=薄荷 `#4a9b8c`；官网对外主题按运行版（暖米白 `#f5f4f0` + 砖橙 CTA） | DESIGN.md 按此分层写，不撒谎 |

### 收敛路径（B/C 待决，暂默认 A 不擅自选）
- **A（当前默认）**：双形态、单一运行源——worktree Nuxt 开发 → 构建产物/源码同步进主要备份 Vite 运行源；文档按"开发版/运行版"双形态写。最稳、不撒谎。
- **B 收敛到 Vite**：正式废弃 Nuxt 官网，前端统一 Vite 运行版结构，worktree 仅作归档。代价=一次较大重构。
- **C 收敛到 Nuxt**：撤 3000 的 Vite 运行版，回 worktree Nuxt。代价=推翻 09-07 运维索引决策。

> **T4 裁定 = 采用 A（默认）**，B/C 列 `TODO.md` P0 待决，待用户拍板后再收敛；在此之前文档一律按双形态分层写，避免任何"官网=Nuxt"或"官网=Vite"的单一撒谎口径。

## 3. 质量部门核验门禁（T4 收口条件）

| # | 核验项 | 通过判据 | 当前 |
|---|---|---|---|
| Q1 | 双真相源物理并存 | worktree Nuxt + 主要备份 Vite 运行版均存在 | ✅ LS 实取 |
| Q2 | 话题→真源映射表 | 本文 §2 映射，8 件套/AGENTS.md 引用同一张表 | ✅ 本文件固化 |
| Q3 | 主色分层 | 后台薄荷 / 官网运行版暖白砖橙，DESIGN.md 分层表述 | ⏳ 待 DESIGN.md 同步 |
| Q4 | B/C 待决不擅自收敛 | 默认 A，B/C 列 TODO P0 | ✅ 本文件 §2 |
| Q5 | 运行源 git 无提交=运行副本 | 改主要备份 frontend 前须回同步源 | ✅ 标注 |

> **T4 收口判定**：Q1/Q2/Q4/Q5 已固化 ✅；Q3（DESIGN.md 分层主色同步）需随 T6 全量回归一并核验，暂记 ⏳ 不阻塞 T4 收口（属文档同步项，非门禁破坏项）。

## 4. 与 100% 口径（B1）的关系

T4 属"代码/文档口径"层，不涉人工阻塞项。T4 通过 = 双形态口径在 8 件套/AGENTS.md/DESIGN.md 一致，且任何模型不得再锚死单一官网技术栈。

*EOF · T4 决策 · 2026-09-12 · 取证源：LS 实取（主要备份 frontend + worktree frontend）+ 8件套收敛设计 + 裁定书 F1/F2*
