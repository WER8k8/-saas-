# 组装总方案 · 复核补遗与勘误 · 2026-09-11> **📌 2026-09-11 19:19 起本册已并入《优丁全项目串联组装总案-合订版-2026-09-11.md》——后续修订以合订版为唯一工作版本，本册为历史分册存档。**> **文件性质**：`优丁全项目串联组装总方案-2026-09-11.md` 的复核订正与技术指导补遗> **复核方式**：逐项对照 worktree 实际代码树验证（非引用旧文档），全部结论可重跑复现> **总体结论**：方案骨架成立，P0–P6 七阶段与关键路径正确；但 **4 处事实性偏差需订正**、**3 处需补强**，其中 1 处为方案未覆盖的商用法律风险。---## 一、勘误表（以实测代码为准）### E1 · Alembic 单 head = 109 ✅（方案正确，旧记忆口径作废）- 实测链：`105 ← 104`，`107 ← c6b6225da363`，`108 ← 107`，**`109_rls_batch2_pilot_reapply ← 108`**，且全库无任何迁移 down 指向 109。- **结论**：单 head = `109_rls_batch2_pilot_reapply`，方案 §3 ENV-LOCK-01 写法正确。- ⚠ 记忆中"单 head 105"为 09-10 旧口径，**作废**，以 109 为准。### E2 · P1-7「补 orders 路由」描述错误 → 改为「打通 orders 前后端最后一公里」- 方案原文说 orders 路由缺失（`routes/orders.py` 不存在）。实测：  - **后端路由已存在**：`backend/app/api/v1/orders.py`（v1 顶层散落文件，`auto_discovery` 机制扫描 v1 顶层 `.py` 自动挂载，无需 `routes/orders.py`）；  - **order_executor 已存在且已注册**（`executors/order_executor.py`，`__init__.py` 已 import）；  - **前端已有 3 处调用 `/api/v1/orders`**：`useQueueBadges.ts`、`views/client/queues/fulfillment.vue`、`views/logistics/orders-track.vue`；  - **真正缺失**：`frontend/admin/src/api/orders.ts` 客户端封装不存在（3 处调用是散写 URL）。- **订正后任务**：P1-7 改为 ① 抽取 `api/orders.ts` 统一封装；② 三处散写调用改为走统一客户端；③ 用 `orchestration_selfcheck` 实测 orders 路由 401（已挂载判定）。### E3 · Phase 6 门禁引用了不存在的测试文件（先建后跑）- 方案 §9.1 写 `python -m pytest tests/unit/test_billing_executor.py`。实测 `backend/tests/unit/`（34 个文件）**无任何 billing/wallet/egress 测试**。- **订正**：该门禁性质是"新建后跑"——P6-1 交付时必须**同步新建** `tests/unit/test_billing_executor.py`（覆盖：正常扣费写入 `token_ledger_entries` / 余额不足返回 `budget_exceeded` / mock 模式诚实降级三用例），门禁才可执行。同理 §9.1 各门禁在执行前先 `ls` 确认脚本存在。### E4 · 工具脚本运行目录混淆（高风险，容易跑错位置）方案所有验证命令统一写 `python tools/xxx.py`，但实测工具脚本**不在 worktree 内**：| 脚本 | 实际位置 | 运行目录 ||---|---|---|| `verify_executors.py` / `capability_ledger.py` / `smoke_orchestration.py` / `db_probe.py` / `fake_scan.py` / `seed_dev_users.py` / `probe.py` / `code_atlas_gen.py` / `build_topology.py` / `youding_acp.py` | **工作区根 `tools/`** | `C:\Users\Administrator\Documents\上线网站开发完成` || `orchestration_selfcheck.py` | **worktree `backend/scripts/`** | worktree 根 || `seed.py` | **worktree `backend/seed.py`** | worktree 根（用 `backend/.venv/Scripts/python.exe`） |- **订正**：执行方案任务时严格按下表切目录；在 worktree 根跑 `python tools/verify_executors.py` 会直接报 `No such file`，反过来在工作区根跑 `backend/scripts/orchestration_selfcheck.py` 也会报错。建议给 `tools/` 脚本统一加 `--worktree` 参数或在文档头部注明期望 cwd。---## 二、P1-9 persona_ref 悬空 · 代码级修复指导（实测定位）**问题确认（非猜测，已实测）**：- 消费端：`backend/app/services/tasks/hermes_task_bridge.py:462` → `node = TaskNode(..., persona_ref=data.get("persona_ref"))`；- 定义端：`backend/app/schemas/hermes_orchestration.py:27` 的 `TaskNode` **没有 `persona_ref` 字段**，且类未配置 `model_config = ConfigDict(extra=...)`；- Pydantic v2 默认 `extra='ignore'`：构造时不报错，但 **persona_ref 被静默丢弃** → 下游执行器永远拿不到 persona。这正是"悬空"的机理，且属于**静默数据丢失**（比崩溃更隐蔽，违反"不假交付"精神）。**修复三步（Phase 1 P1-9 执行）**：```python# ① schemas/hermes_orchestration.py — TaskNode 增加字段（sop_ref 之后）：persona_ref: Optional[str] = Field(None, description="AgencyZH persona role id, resolved by role_loader at runtime")# ② planner_service.py — 拆解时按意图产出 persona_ref（至少给 accio/research 节点赋默认角色），#    模板场景（templates/outreach_scenario.py 等）中补 persona_ref 键。# ③ 执行器侧（base.py 或 role_loader.py）— ExecutorContext 增加 persona 解析钩子：#    context.resolve_persona(node.persona_ref) -> PersonaDefinition | None#    persona_ref 为 None 或查不到时返回 None 并记录日志，禁止编造默认 persona（不假交付）。```**验证**：构造带 `persona_ref="role:trade_writer"` 的 TaskNode → 断言 `node.persona_ref` 不为 None；查不到的 ref 断言执行器日志出现 `persona not found, degraded` 而非假成功。---## 三、补强 1 · TradeAI 许可证风险（方案 §8 未列，商用前必须处理）实测 `backend/app/services/adapters/tradeai/README.md` 明确记载：> Trade AI Agent 来源 Gitee `LBones-li/agent_trade_b`，README 声明 MIT 但**根目录无 LICENSE 文件**——商用合入前必须向作者书面确认（总纲 §9.4-1），未完成前不得对外分发含本适配器的版本。**处置**：1. §8 风险表增加一行：`TradeAI 适配器许可证未确认 | 商用分发法律风险 | P3-2 动工前向作者取得书面授权或保留出处注释 + 律师复核；未确认前该适配器版本锁内测`。2. P3-2 增加前置条件：授权确认截图归档到 `00-文档中心/审计/`。## 四、补强 2 · "丝滑流畅"专项（系统体验保障指导）装配期最容易把系统做"卡"，以下作为各阶段横向约束（并入每个 Phase 出口自检）：| 层 | 约束 | 落点 ||---|---|---|| DB | PG@5433 连接池上限与 Celery worker 并发匹配（worker 数 × 并发 ≤ pool 上限），防止连接耗尽全站转圈；229 表批量 seed 用分批 commit（每 500 行），避免长事务锁表 | `config/dev/.env`、`seed.py` || 编排 | 24 执行器并发默认 `max_parallel: 3`（GraphPolicies 已有）；单节点 `TaskBudget` 必填上限，防一个平台桩死循环拖垮整图；失败节点走 `on_fail: degrade/skip` 而非 abort 全图 | `schemas/hermes_orchestration.py` || 前端 | 49 孤儿页并入路由时**一律路由级懒加载**（`() => import(...)`），防止主包膨胀首屏变慢；orders-track 等轮询页轮询间隔 ≥10s 且页面隐藏时暂停 | `router/index.ts` || 出站 | 42 平台接线必须串行 tier 分批放量，浏览器 runtime 复用 profile 会话，禁止每任务冷启动一个浏览器（内存爆） | `tier_router.py`、`browser_executor.py` || 可观测 | 全链路 trace_id 已打通——每个 Phase 收尾抽查 1 条真实任务的 trace 链路完整性，作为"丝滑"的量化证据 | `AiTask.output_json` |## 五、补强 3 · 门禁存在性预检脚本（防跑错/防引用不存在的脚本）E3/E4 的根因是"门禁引用未先验存在"。给出一次性预检命令（在**工作区根**执行）：```bash# 逐阶段门禁前置检查：存在才跑，缺失则标记 BUILD-FIRSTfor f in tools/verify_executors.py tools/smoke_orchestration.py tools/capability_ledger.py \         tools/db_probe.py tools/fake_scan.py tools/probe.py; do  [ -f "$f" ] && echo "OK    $f" || echo "MISS  $f"donecd 上线网站.worktrees/agents-install-vscode-cline-deploy-strix && \  { [ -f backend/scripts/orchestration_selfcheck.py ] && echo "OK    orchestration_selfcheck" ; \    [ -f backend/seed.py ] && echo "OK    seed.py" ; }```建议把该清单写进 `development.md` 回归清单节，作为每个 Phase 门禁的第 0 步。---## 六、订正对执行顺序的影响- **P0–P6 阶段划分、关键路径、依赖关系：全部维持不变**（勘误均为任务内容级，未动摇骨架）。- P1-7 内容改写（E2）后，其验证从"orders 表可写"升级为"前端统一客户端 + 后端 401 挂载判定 + orders 表可写"三项。- P3-2 增加许可证前置条件（补强 1），**P3 整体不阻塞**：P3-1/P3-3/P3-4/P3-5/P3-6 可先行，仅 `adapters/tradeai` 代码合入对外分发受限。- 下一步三件事（方案 §10）维持不变，即刻可执行。---*本补遗由主理人复核要求触发，全部结论基于 2026-09-11 实测代码树（文件级证据），与总方案配合阅读。*