# 代码像素级盘点 · 文件通道核验 · 2026-09-10

> **方式**：**bash 通道挂断期间**，用 host 文件通道（Read/Glob/Grep）对代码做逐文件存在性 + 关键结构核验。**未运行任何脚本**（bash 不可用）。
> **目的**：回答「磁盘上到底有哪些关键代码、结构是否完整」。数值类（串联率、表数、行数）**仍以 bash 恢复后重跑为准**。
> **结论**：**编排链关键路径在磁盘上完整存在**；发现 **2 个非本会话生成的新文件**（可能来自并发模型），需注意。

---

## 一、核心链路（逐文件核验）

| 组件 | 路径 | 状态 |
|---|---|---|
| 执行器基类 | `backend/app/services/hermes/executors/base.py` | ✅ 存在 |
| 执行器包 | `backend/app/services/hermes/executors/__init__.py` | ✅ 存在，9 个 import 全在 |
| 执行器 · 9 个 | `accio / content / deerflow / egress / goodjob_crm / nurture / publish / site_builder / trade_ai_agent` | ✅ 9 个文件全在 |
| 注册点 · 9 个 | 每个 `*_executor.py` 底部 `ExecutorRegistry.register(...)` | ✅ 9 处全在 |
| 拆解器 | `backend/app/services/hermes/planner_service.py` | ✅ 存在（`decompose` / `known_capabilities` 命中） |
| 调度内核 | `backend/app/services/hermes/task_control_supervisor.py` | ✅ 存在（`_capability_matches` 命中） |
| 编排路由 | `backend/app/api/v1/routes/orchestration.py` | ✅ 存在（`/tasks/from-intent` 命中） |
| 前端 API | `frontend/admin/src/api/orchestration.ts` | ✅ 存在 |
| ACP 服务端 | `tools/youding_acp.py` | ✅ 存在（引用 from-intent） |
| 冒烟脚本 | `tools/smoke_orchestration.py` | ✅ 存在（引用 from-intent） |

**结论：我给 Codex 交接的「编排链主干」在磁盘上真实齐全。**（与总表审校一致，N.5-N.9 确已接入。）

---

## 二、工具脚本（tools/ 14 个）

| 脚本 | 本会话所建 | 状态 |
|---|---|---|
| `code_atlas_gen.py` / `code_atlas_md.py` | ✅ | ✅ |
| `capability_ledger.py` / `capability_tags.py` | ✅ | ✅ |
| `verify_executors.py` / `fake_scan.py` | ✅ | ✅ |
| `smoke_orchestration.py` | ✅ | ✅ |
| `db_probe.py` / `probe.py` | ✅ | ✅ |
| `skills_index_rebuild.py` | ✅ | ✅ |
| `seed_dev_users.py` | ✅ | ✅ |
| `youding_acp.py` | ✅ | ✅ |
| **`dbconn.py`** | ⚠️ **非本会话生成** | 待验证 |
| **`verify_dispatch.py`** | ⚠️ **非本会话生成**（总表 H.1 提过此脚本） | 待验证 |

> ⚠️ **并发修改警告**：`dbconn.py` / `verify_dispatch.py` 不是我这个会话写的——**说明有另一个模型（很可能是 Codex）在本工作区并行作业**。这没有错，但请确认：
> 1. 这两个文件是谁写的、是否可运行（bash 恢复后跑 `python3 -m py_compile` 验证）
> 2. 并发写文件时**不会互相覆盖**（尤其 `executors/` 和 `tools/`）

---

## 三、已确认的「待 bash 恢复后重跑」清单

| 项 | 为什么 |
|---|---|
| `code_atlas_gen.py` / `code_atlas_md.py` 重生成代码地图 | 新执行器加入后 atlas.json 过期 |
| `capability_ledger.py` 刷新串联率 | 当前 15.3% 是旧值 |
| `verify_executors.py`（9→N） | 确认新增文件不破坏契约 |
| `fake_scan.py`（HIGH 0） | 门禁之一 |
| `smoke_orchestration.py --live` | **端到端冒烟，Phase 2 核心** |
| `verify_dispatch.py` / `dbconn.py` | 未知来源文件，先 py_compile + 用途确认 |

---

## 四、结论

**磁盘是健康的**：我交接的核心链路（拆解器→调度→9 执行器→前端/A⟌ACP）全部真实存在、注册点闭合。**代码没有凭空消失**。

**但有两件事需要你知道**：
1. `dbconn.py` / `verify_dispatch.py` **不是我建的**——请确认来源（大概率是 Codex），并跑 py_compile。
2. **端到端仍是「未验证」**——磁盘健康 ≠ 跑的通。bash 恢复后第一件事是 `smoke_orchestration.py --live`。

---

*本盘点为文件通道核验（bash 挂断期间）。数值以 bash 恢复后重跑为准。*