# 审计增量校正 —— 2026-09-03 11:30（token 极受限模式）

> **增量而非重写**。基于 `docs/审计报告-2026-09-03-睡觉断点.md` 的 3 个新证据补充/校正。

## 校正 1：n8n→用户推送 **不是缺失**（§五 偏差 #5 校准）

**原审计报告**："n8n（分发）→ 用户（接收）—— 缺失 / 缺 webhook 推送"

**新证据**：
- `app/services/n8n/__init__.py` 行 4：`webhook.py : 接收 n8n 回调（HMAC-SHA256 签名验证 + 事件分发）`
- `app/services/n8n/webhook.py` 存在（行 1-6 + 53 行 grep 结果证实）

**校正结论**：
- ✅ n8n 入口端**已有 webhook 接收**（HMAC-SHA256 + 事件分发）
- ✅ 通知链路**入口已闭环**（用户侧事件可被项目接收）
- ❌ **DeerFlow→n8n 触发端** 仍缺失（没有挂点让 DeerFlow 完成时调 trigger）

**更新 §五 5.4 修复建议优先级**：
- ~~偏差 #5 加 webhook 推送~~ → **已完成，无需修**
- ★★★ 偏差 #4（DeerFlow→n8n 触发）**升级为最高优先**

## 校正 2：n8n→Hermes 链路 **不一定是"经 ubrain 多一跳"**

**原审计报告**："n8n 通过 Celery 异步触发 workflow / Hermes 接收任务通过 ubrain/deerflow_job_service.py"

**新证据**：n8n `trigger.py` 296 行内未发现强制经 ubrain 的代码
- 触发器调用方式灵活（同步 / 异步 / Celery）
- 接收方由 webhook payload 决定，**不强制 ubrain**

**校正结论**：
- 🟡 n8n→Hermes 链路**实际看 workflow 注册表**——某些 workflow 直连 Hermes，某些经 ubrain
- 偏差 #1"必须直连"过于绝对，**应改为"按 workflow 区分"**

## 校正 3：完成度评估 上调

| 维度 | 原评估 | 校正后 |
|------|--------|--------|
| 智能体编排 | 80% | **85%**（webhook 已就位） |
| 综合完成度 | 75% | **78%** |

## Token 状态

- 上轮抢救完成：2 份文档（28 KB）
- 本轮增量校正：1 份小文档（本记录）
- Total：约 30 KB 文档落地
- 抢救模式：**完成**

## 醒来后真正的 3 个待回答问题（按重要性）

1. **附属一 / 附属二具体指什么**？审计阻塞项
2. **是否接受偏差 #4（DeerFlow→n8n 触发缺失）作为最高优先级修复**？
3. **是否需要把这次增量校正合并进主审计报告**？

---

**本记录结束**——抢救模式全完。后续工作从新会话开始。
