# 开发交接记录 · 全维度资产盘点审计（2026-09-11）

> 自包含交接手册：给下一个 IDE / 模型 / 同事。读完即可接手，无需重问背景。
> 配套审计：`docs/资产盘点审计-全维度-2026-09-11.md` + `tools/asset_audit_report.json`（机读）+ `tools/asset_audit_scan.py`（重跑）。

---

## 1. 本次做了什么

对 UJ/优丁项目做了**全面资产盘点与审计**（纯分析，未改业务代码）：
- 编写并重跑 `tools/asset_audit_scan.py`，真实扫描 45,350 个源码文件（排除 node_modules/.git/venv/压缩包/`主要备份` 只读存档）。
- 产出**112 维统计**（规模/结构/后端/前端/子系统/完成度/依赖/生命周期/质量风险）。
- 逐子系统记录：功能作用、依赖关系（被依赖/依赖）、调度调用、父子层级、完成度。
- 统计半成品/搁置/遗忘项：保守完成度标记 12,681（TODO 4,802 / NotImplemented 1,054 / STUB 1,479 等）；历史欠账（串联率 15.3% / 122 未串 / 49 孤儿页 / 42 桩平台 / 218 空表）。

## 2. 当前真实状态（红线 10 条）

1. **数据库**：原生 PostgreSQL 15.8 @5433（229 表，单 head 105），**仅 11 张有数据，业务表全空**；Redis 5.0 @6379；n8n @5678（2 webhook 激活）。
2. **端口**：8000 被 Apache/PHP 占用 → 后端起在 **8001**；要外部访问须 `start-lan.bat`（绑 0.0.0.0）。
3. **env 优先级**：OS 环境变量 > `backend/.env` > `backend/config/dev/.env`；`backend/config/.env.dev` 是孤儿文件（永不读取）。
4. **沙箱直连宿主**：用 `172.16.10.254:5433`，**勿用 `host.docker.internal`**（Clash fake-IP 掐断）。
5. **双官网**：worktree Nuxt3（薄荷）为开发源；`主要备份/上线网站/frontend` Vite（暖白/砖橙）为 3000 实跑源；收敛未决。
6. **八大子系统不可裁减**（SYSTEM-LOCK-02）：Core/Hermes/DeerFlow/TradeAI/GoodJob/350+资产/DeepSeek/出站，严禁漏或退役。
7. **三硬锁**：LOGIN-LOCK-01（唯一 `/login`）、ROLE-SHELL-LOCK-01（四壳固定）、DESIGN-TOKEN-LOCK-01（薄荷 `#4a9b8c` 单真源）。
8. **开发方式**：禁手敲 uvicorn（漏 REDIS_ENABLED）；禁代码树内存备份（放 `_archive/`）。
9. **编排链**：拆解器已落、9 执行器在盘；但 **trade_ai_agent / goodjob_crm adapter 仍为未真接桩**；端到端冒烟（`smoke_orchestration.py --live`）未跑通，不得宣称链路已通。
10. **门禁**：任何编排改动须 `orchestration_selfcheck.py` 全绿（项数以实跑为唯一真值）。

## 3. 关键数字速查

| 指标 | 值 |
|---|---|
| 源码文件 / 估算 LOC | 45,350 / 9.5M |
| 后端路由装饰器 / 模型 / Schema / Celery | 2,055 / 238 / 701 / 32 |
| 八大子系统 | 8（全在位，部分未真接） |
| 保守完成度标记合计 | 12,681（TODO 4,802 / NotImplemented 1,054 / STUB 1,479 / FIXME 263 / XXX 1,049 / HACK 34）|
| 串联率 / 未串模块 | 15.3% / 122 of 150 |
| 孤儿页 / 孤儿组件 / 桩平台 / 空表 | 49 / 11 / 42 / 218 |
| 前端测试文件 | 0 ⚠ |
| 硬编码凭证命中 / 假货命中 | ~32 / ~308 |

## 4. 接手第一件事（先验证再下结论）

```bash
# 1) 现状体检（任何结论前先跑）
python tools/probe.py
python tools/db_probe.py --density
# 2) 重跑本次审计（复算数字）
python tools/asset_audit_scan.py
# 3) 起服务（LAN 模式，绑 0.0.0.0）
启动脚本\youding-dev-start.bat -Core
# 4) 端到端冒烟（核心卡点：服务未起 + 业务表空）
python tools/smoke_orchestration.py --live
```

## 5. 推荐下一步（按优先级）

- **P0**：补前端测试基线（vitest + typecheck + lint，CI 强制）；清理 ~32 处硬编码凭证（迁入 `.env`，`fake_scan.py` 复查）。
- **P0**：打通端到端冒烟（先起服务 + seed 业务数据，再 `smoke_orchestration.py --live`）。
- **P1**：推进 Trade AI / GoodJob adapter 真接入（当前 `ready_for_adapter` 桩）。
- **P1**：周期跑 `fake_scan.py` 清散落假货；归档工作树 200+ 散落调试脚本至 `_archive/`。
- **P2**：双官网收敛决策（A/B/C）落地；ECC 286 SOP 默认关闭策略复核。
- **P3**：218 空表补数据验证；49 孤儿页可达性修复。

## 6. 文档地图（本次新增 / 关联）

- 新增：`docs/资产盘点审计-全维度-2026-09-11.md`（全维度档案）
- 新增：`tools/asset_audit_scan.py`（重跑脚本）、`tools/asset_audit_report.json`（机读）
- 关联：`docs/00-会话产出串联索引-2026-09-10.md`（全脉络）、`未驱动串联资产盘点-2026-09-11.md`（串联欠账）、`AEOS_MASTER_CODEBASE_INDEX.md`（系统字典）、`AEOS_GLOBAL_SYSTEM_REGISTRY.json`（机器注册表）、`后端运行环境强制索引-必读.md`、`官网启动强制索引-必读.md`

## 7. Obsidian 结构化本体同步（obsidian-ontology-sync）

审计结论已同步为结构化本体，自动经 NTFS Junction 进入 Obsidian Vault（`01-权威文档与系统全景/ontology/`）：
- `docs/ontology/优丁资产审计本体-2026-09-11.md` —— 本体 MOC（Project 实体，frontmatter + wikilink）
- `docs/ontology/subsystem_*.md` ×8 —— 八大子系统节点
- `docs/ontology/risk_*.md` ×5 / `task_*.md` ×4 —— 风险与下一步任务节点
- `docs/ontology/audit-graph-2026-09-11.jsonl` —— 机器可读本体图（upsert/relate，兼容 skill `sync.py` 的 analyze/feedback）
- 重生成：`python tools/gen_audit_ontology.py`（数字来自 `asset_audit_report.json`）

> 注：文件写入 `docs/` 即自动同步 Vault，无需手动复制；jsonl 为 DERIVED 图，供 obsidian-ontology-sync 查询/反馈。

*交接人：主理人齐活林（Qi）· 2026-09-11*
