# 任务看板 · 编排链续作

> **这块板子的用途**：让你随时知道"现在在干什么、干完没有、卡在哪"。
> **规矩**：每完成一项当场改状态，不许攒到最后一次性勾完。
> **最后更新**：2026-09-10 16:35（每完成一项当场改状态）

---

## 一、一句话现状

**编排链第一次真跑通了，7 个节点里 4 个真完成；卡在"开发环境没有微信/知乎账号"，不是代码坏了。**

---

## 二、四个门禁的最新真实数字

| 门禁 | 要求 | 实测（15:56） | 状态 |
|---|---|---|:--:|
| `tools/fake_scan.py` | HIGH = 0 | HIGH = 0 | ✅ |
| `tools/verify_executors.py` | 全绿 | 合规 9/9 | ✅ |
| `tools/smoke_orchestration.py`（静态） | 全绿 | 模板引用全注册 | ✅ |
| `tools/capability_ledger.py` | 串联率不下降 | 按模块 15.3% ｜ 按端点 14.2% | ✅ 基线 |

> 附：端到端 `--live --dispatch --approve` 最好成绩 = **4/7 节点 done**（15:45 那轮）。

---

## 三、任务清单

### A. 让现有链路真跑起来（已完成）

| # | 任务 | 状态 | 备注 |
|---|---|:--:|---|
| A1 | 修 9 个门禁脚本的写死路径 | ✅ 12:15 | 原来全指向一台不存在的 Linux 机器 |
| A2 | 修登录字段 + 去掉硬编码账号 | ✅ 12:15 | 改 `username_or_email`；账号只能命令行传 |
| A3 | 查数据库确认账号-租户关联 | ✅ 14:05 | `admin`/`agent` 无关联行，冒烟只能用 `tenant` |
| A4 | 写 `db_probe.py` 看节点真实状态与真实产出 | ✅ 14:30 | 日志被缓冲，只能查库判进度 |
| A5 | 修「任务失败后永久卡在执行中」 | ✅ 15:20 | 真 bug 1 |
| A6 | 修「DeerFlow 能力名与编排对不上」 | ✅ 15:03 | 真 bug 2：等于两根线头没接 |
| A7 | **修「审批闸在取上游内容之前」** | ✅ 15:35 | 真 bug 3（最关键）：放行后执行器拿不到正文 |
| A8 | 修「放行只改状态不重新派活」 | ✅ 15:20 | 真 bug 4 |
| A9 | 补 3 个中文回归测试 | ✅ 15:42 | 28 个用例全绿，不连真库 |
| A10 | 端到端真跑 | ✅ 15:45 | 4/7 done，第 4 节点真去调外部发布 |

### B. 当前卡点（需要你拍板，但我先按"不假交付"处理）

| # | 任务 | 状态 | 备注 |
|---|---|:--:|---|
| B1 | 发布节点区分「没配置」与「配置了但发失败」 | 🟡 进行中 | 前者标 skipped 并写明原因，后者仍标 failed |
| B2 | 重跑端到端到 7/7 done（或 6 done + 1 明确 skipped） | ⛔ 待 B1 | |

### C. 按早上简报顺序补执行器（112 个待串模块的头部）

| # | 批次 | 接什么 | 状态 |
|---|---|---|:--:|
| C1 | 第 1 批 | `media`（图片/视频，业务链第④⑤步） | ⛔ 未开始 |
| C2 | 第 2 批 | `seo`（`seo_matrix` 59 端点，最大未串模块） | ⛔ 未开始 |
| C3 | 第 3 批 | `lead`（`lead_generation` / `lead_search`） | ⛔ 未开始 |
| C4 | 第 4 批 | `crm`（`crm_pipeline` / `opportunity`） | ⛔ 未开始 |
| C5 | 补空白 | `order`（表在、路由模块没有，全链唯一真空白） | ⛔ 未开始 |

### D. 文档留痕（不许拖到最后）

| # | 任务 | 状态 |
|---|---|:--:|
| D1 | 订正总纲 §6 阶段表里三处过期数字（只改数字不动设计） | ✅ 16:33 |
| D2 | `编排主链打通实作记录` 补「已验证」+ 如实记「未验证」 | ⛔ |
| D3 | `00-会话产出串联索引` 追加本期产出 | ⛔ |
| D4 | `.workbuddy/memory/2026-09-10` 追加一条 | ⛔ |
| D5 | 清理 `scratch/` 临时脚本与日志 | ⛔ 门禁全过后 |

---

## 四、发现的设计文档自相矛盾（记录，不擅自改设计判断）

1. 总纲 §6 的 P1 写「待建 `decompose()`」，实测该函数**已在** `planner_service.py:371`，且 `/orchestration/tasks/from-intent` 已真接通。
2. 总纲 §6 的 P2 写「封装 9 个执行器」，但 9 个已存在；真实工作量是归属表里那 **112 个 `to-orchestrate`**。
3. 总纲 §6 的 P3 写「补自动规则空白」，而同文档 §2 已勘误「摇号→养号」且养号是**真实现**（33KB 服务 + 调度器 + 8 端点），并非空白。

---

## 五、硬锁自检（每轮收尾确认）

| 锁 | 是否碰过 |
|---|:--:|
| 唯一登录 `/login` | 未碰 ✅ |
| 四壳固定 | 未碰 ✅ |
| 主色薄荷 `#4a9b8c` | 未碰 ✅ |
| 编排契约（schema / BaseExecutor / planner） | 未碰 ✅ |
| 八大子系统不裁减 | 未碰 ✅ |
| PG@5433 唯一真源 / 禁手敲 uvicorn | 守住 ✅ |
| 不写假数据 / 不假成功 | 守住 ✅ |
