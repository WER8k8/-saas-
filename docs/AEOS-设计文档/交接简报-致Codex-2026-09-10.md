# 交接简报 · 致 Codex（优丁编排链续作）

> **交接方**：Claude ｜ **日期**：2026-09-10 ｜ **工作区**：`C:\Users\Administrator\Documents\上线网站开发完成`
> **性质**：本文件是自包含交接简报，读完即可开工。**开工前请先跑一次"现状核对"，不要凭本文件描述直接改代码。**

---

## 0. 你接手时的状态（一句话）

**编排链的代码已经打通，但端到端没跑过。** 所以你的第一件事不是写新代码，是**验证现有代码真能跑**。

---

## 1. 先做这三件事（按序，别跳）

### ① 核对现状（只读，5 分钟）

```bash
cd "C:\Users\Administrator\Documents\上线网站开发完成"

# 执行器契约（应为 9/9 合规 + 21 能力，无冲突报警除 content.create）
python3 tools/verify_executors.py

# 假交付扫描（应为 HIGH 0 / MED 0）
python3 tools/fake_scan.py

# 编排链静态检查（应为全部通过）
python3 tools/smoke_orchestration.py

# 能力台账（当前串联率应为 15.3%）
python3 tools/capability_ledger.py
```

**四条全过 → 说明代码库处于我交接时的状态。有不过的 → 先查是不是我之后有人改过。**

### ② 起服务

**必须用 LAN 模式**（否则后端只绑 127.0.0.1，外部访问不到）：

```
双击工作区根的 start-lan.bat
```
或 `powershell -ExecutionPolicy Bypass -File "上线网站.worktrees\agents-install-vscode-cline-deploy-strix\scripts\start-dev-lan.ps1" -ForceRestart`

起来后：后端 **8001**、admin **5173**、官网 **3000**。

### ③ ★ 跑端到端冒烟（**这是本次交接的核心任务**）

```bash
python3 tools/smoke_orchestration.py --live
```

它会自动：探活 → 登录（`admin/admin123`）→ 提交「帮我建站并推广」→ 校验拆解结果 → 回查落库。

**预期**：`graph_source` 为 `L1_template`（模板命中），`node_count` ≈ 7，`dispatched=true`。

**结果分两种，处理方式不同**：

| 结果 | 含义 | 你该做什么 |
|---|---|---|
| ✅ 全绿 | 编排链真的通了 | 进入第 2 节「继续串联」 |
| ❌ 报错 | **有真 bug** | 定位修复。**这是最有价值的活**——修完把根因写进 `docs/编排主链打通实作记录-2026-09-10.md` 的「未验证」节 |

> **⚠️ 注意**：数据库里的业务表**全是 0 行**（229 表中仅 11 张有数据，`users` 原本也是 0 —— 已补 9 个账号）。如果冒烟卡在"没有数据"，先跑 `backend/seed.py` 灌业务种子。

---

## 2. 验证通过后：继续串联（照单执行，不要自由设计）

**不要重新设计架构。** 设计与接线方案都已就绪，你的工作是**照单把执行器一个个接上**。

### 数据来源（告诉我你还差什么）

| 问题 | 读哪份 |
|---|---|
| 哪些模块该串还没串？ | `docs/能力台账/归属表.md`（找 `to-orchestrate`，当前 112 个） |
| 每个模块怎么串？ | `docs/能力串联接线图-2026-09-10.md` |
| 50 个平台怎么接？ | `docs/50平台接线台账-2026-09-10.md`（当前 8 真发 / 42 待接，分 5 批） |

### 照抄模板（**关键**）

新执行器**照 `backend/app/services/hermes/executors/site_builder_executor.py` 写**，它是最干净的一个：

```python
class XxxExecutor(BaseExecutor):
    @classmethod
    def get_executor_name(cls) -> str:
        return "xxx"

    async def run(self, node: TaskNode, context: ExecutorContext) -> ExecutorResult:
        # node.input 已含 advance_plan 注入的 input_from 解析结果
        ...
        return ExecutorResult(node_id=node.id, status="succeeded", output={...})

    @classmethod
    def get_capabilities(cls) -> Dict[str, Dict[str, Any]]:
        return {"xxx.capability": {"desc": "...", "input": [...], "output": [...]}}

ExecutorRegistry.register(XxxExecutor())
```

然后在 `executors/__init__.py` **加一行 import**（不加等于没注册）。

### 建议的下一批顺序

| 批次 | 接什么 | 为什么 |
|---|---|---|
| **第 1 批** | `media`（图片/视频，`media_factory` + `video_publish`） | 业务链第④⑤步，服务已真 |
| **第 2 批** | `seo`（`seo_matrix`，59 个端点，**当前最大的未串模块**） | 业务链第⑪步 |
| **第 3 批** | `lead`（`lead_generation` / `lead_search`） | 业务链第⑩步 |
| **第 4 批** | `crm`（`crm_pipeline` / `opportunity`） | 业务链第⑭⑮步 |
| **补空白** | **`order`** —— `orders` 表在，但**没有任何路由模块** | 业务链第⑯步，唯一真空白 |

---

## 3. 绝对不许做的事（硬锁，破坏即高优先级回归）

读 `AGENTS.md` §3，六条硬锁全文。**核心几条**：

1. **登录**：只能 `/login`，唯一组件 `frontend/admin/src/views/login/index.vue`。禁止第二套登录页。
2. **四壳**：`/client/*` `/admin` `/partner` `/agent/*`，禁止新建 `views/admin/tenants.vue`、`views/tenant/**`。
3. **主色**：薄荷绿 `#4a9b8c` 单真源（`frontend/admin/src/stores/uiPreferences.ts`）。禁止硬编码 `#2563eb`/`#7c3aed`/`#ea580c`。
4. **八大子系统不可裁减**（SYSTEM-LOCK-02）。
5. **运行环境**：PG@5433 / Redis@6379 / n8n@5678，禁止切回空 SQLite。禁止手敲 `uvicorn`（走启动脚本）。
6. **代码树内禁止放备份文件**（放 `_archive/`）。

### 另外三条（本轮的教训）

- **不许写假数据**。宁可明确 `failed` 并说明缺什么，也不返回编造的成功。跑 `tools/fake_scan.py` 自检，**HIGH 必须为 0**。
- **不许硬编码路径/密钥/账号**。配置走 `env`，参数走变量。
- **不许改契约**。`schemas/hermes_orchestration.py` 的 `IntentEvent`/`TaskGraph`/`TaskNode`/`GraphPolicies`/`ExecutorResult`，以及 `BaseExecutor` 的接口。要加能力就加执行器，不要改契约。

---

## 4. 收尾门禁（跑不过不许声称完成）

每轮改完，**必须**跑：

```bash
python3 tools/verify_executors.py      # 执行器契约 → 必须全合规
python3 tools/fake_scan.py             # 假交付 → HIGH 必须 0
python3 tools/smoke_orchestration.py   # 链路静态 → 必须全过
python3 tools/capability_ledger.py     # 串联率 → 不应下降
```

然后**更新会话索引**（这是项目硬规矩，见 `CLAUDE.md`）：

- `docs/00-会话产出串联索引-2026-09-10.md`
- 新增「会话脉络」行 + 「产出物」条目
- **查漏**：`docs/*<日期>*.md` + `tools/*.py|.bat` 逐个比对，**未收录数必须为 0**

---

## 5. 已知的坑（别踩）

| 坑 | 说明 |
|---|---|
| **数据库业务表全空** | 229 表仅 11 张有数据；需跑 `backend/seed.py` 灌种子 |
| **`host.docker.internal` 不可用** | 沙箱/远程访问宿主用 `172.16.10.254`（Clash fake-IP 会掐断连接） |
| **8000 端口被占** | 是 Apache/PHP(FunAdmin)，非本项目后端；本项目后端在 **8001** |
| **env 优先级** | ① OS 环境变量 ② `backend/.env` ③ `backend/config/dev/.env`；`backend/config/.env.dev` **是孤儿文件，永不读取** |
| **旧模板有坏引用** | `templates/site_build_scenario.py` 引用了 `seo_matrix`/`ai_engine`/`bullmq_seo_backend`/`n8n_webhook`（**均不存在**）；`outreach_scenario.py` 引用 `temporal_workflow`（不存在）。**新 planner 不消费它们**，但别照抄 |
| **`persona_ref` 被静默丢弃** | `hermes_task_bridge.py:171` 传了但 schema 无此字段（pydantic 忽略，不崩） |
| **worktree `.git` 链接损坏** | 指向旧用户 Profile 的不存在路径，在 worktree 内跑 git 命令会失败 |
| **`content.create` 能力冲突** | 被 `content` 与 `deerflow` 同时声明（注册表保留先注册者并告警） |

---

## 6. 交接时的未验证清单（**你的第一批活**）

- [ ] **端到端真跑** —— 所有验证都是静态的，`smoke --live` 没跑过
- [ ] **ACP 服务端连真后端** —— `--selftest` 是 mock 后端，真连需服务起来
- [ ] **`GraphPolicies` 三重闸门真跑** —— 逻辑单测过（8/8），但没在真实任务图上验过
- [ ] **AionUi 工作树补齐** —— blobless 克隆，116/2344 文件；`git -C _external/aionui checkout -f HEAD`
- [ ] **继续查「设计承诺有没有落地」** —— 已知可疑点：节点级 `budget` 字段、`degradation` 策略、`sop_ref` 的 ECC 注入是否真生效

---

## 7. 上下文（你要理解这套东西是干什么的）

**优丁 YouDing** = 面向建材外贸卖家的多租户 B2B SaaS。租户有独立域名官网，用 AI 写内容 → SEO/GEO → 分发到 50 个平台 → 流量回官网 → **询盘到物流的七步闭环**。

**你正在接的这条链**：

```
用户一句话（AionUi / Web / API）
   → decompose()  拆解器（L1模板/L2 LLM/L3兜底 + 三道安全阀）
   → TaskGraph    任务图
   → AiTask       父计划 + N 个子任务（落库）
   → advance_plan 推进（含并发/审批/预算三重闸门）
   → ExecutorRegistry → 9 个执行器
   → 结果回写 + n8n 出站
```

**设计原文**：`docs/架构设计-智能编排内核-任务图驱动-2026-09-06.md`（**项目 09-06 就有这份设计，P1/P2 已做、P3 由我补齐**）
**收口总纲**：`docs/全链路串联设计-总纲-2026-09-10.md`

---

## 8. 一句话给你的开场提示

> 你接手优丁项目的编排链续作。**先读 `docs/模型无关续作规约-防设计漂移-2026-09-10.md`**（防你会跑偏），再读本简报。第一件事是**起服务跑 `python3 tools/smoke_orchestration.py --live` 验证现有链路**，不是写新代码。验证通过后照 `docs/能力台账/归属表.md` 找 `to-orchestrate` 的模块继续接。**每轮收尾必须跑四个门禁脚本 + 更新会话索引，跑不过不许声称完成。**

---

## 9. 2026-09-11 增量：未驱动串联资产盘点（读这份补全工作清单）

> 三路并行子代理源码实测，详见 **`docs/未驱动串联资产盘点-2026-09-11.md`**。前端像素级问题另见 **`docs/官网像素级盘点报告-2026-09-10.html`**（五维 12/20，P0×3）。

### 9.1 你现在手上的"待接清单"（数字版）

| 层 | 总数 | 未驱动 | 备注 |
|---|---:|---:|---|
| 后端路由模块 | 150 | **122** | 81.3% 未串；接法见 `能力台账/归属表.md`（to-orchestrate 112） |
| 前端页面 / 组件 | 78 / 106 | **49 页 + 11 组件** | 孤儿页不可达；并入主站或归档 |
| 发布平台 | 50 | **42** | 84% 桩；接线见 `50平台接线台账-2026-09-10.md` |
| 编排引擎 | 12 | **5** | UBrain/Paperclip/Wangcai/video_publish_orchestrator/workflow_canvas_service 绕过 Hermes |
| 智能体 | 687 | **1** | `services/facade/model_gateway_facade.py`（接线或删） |
| 中间件 | 34 | **1** | `services/vault/`（仅 tests 引用；接线或归档） |
| 外部项目 | 5 | **1** | `_external/aionui`（后端零消费；决定是否走 ACP） |
| 数据库表 | 229 | **218 空** | 灌种子是"真跑"前提：`backend/seed.py` |

### 9.2 追加的三条已知坑

- **`core/celery_app.py` 旧 task 无调度入口**：send_email/score_lead/cleanup 未进新版 `tasks/celery_app.py` include——要复用需补注册。
- **前端孤儿页是蓝色/大圆角/错误滚动的重灾区**（Mobile* 组件群锁死其中）——改前端前先确认页面可达（`router/index.ts` 注册）。
- **前端滚动容器 = #app**：<768px 时 `window.scroll*` 恒失效，必须用 `frontend/composables/useAppScroll.ts`（像素盘点已实测首页锚点失效）。

### 9.3 收尾门禁追加

前端改动增加：`npm run typecheck` + `npm run lint` + `npm run lint:tokens`（令牌门禁，能拦 `#2563eb` 硬编码违规），并在 3000 端口按「首页锚点 / 联系首屏 / 移动视口」回归。

---

*交接方：Claude ｜ 日期：2026-09-10 ｜ 如本简报与代码不符，以代码与实测为准。*
