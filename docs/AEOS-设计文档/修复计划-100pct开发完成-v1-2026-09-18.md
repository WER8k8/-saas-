# 100% 开发完成 · 可执行修复计划 v1（2026-09-18）

> 触发：主理人要求"做不了事=耽误事，必须 100% 修复/开发完成"。
> 前置核实（本会话实测）：登记面串联率 100%，但真实数据面 PG@5433 **239 表中 74.5% 空表**、核心外贸履约/支付/物流等 **22+ 表在库里查无此表**，源码层 mock 489 / degraded 130 / 未配置 285。
> 本计划为"先出计划→确认→再动工"。真实外部资源（翻译/支付/WhatsApp/社媒/LLM Key）**已有，可按序接线**。

---

## 0. 目标定义（"完成 = 绿"的可验收标准）
- 数据面：缺失核心表建成并迁移到 PG@5433，业务写路径可落库，空表比例显著下降（除系统级/预留表）
- 逻辑面：空转执行器补真实现或显式硬化；不存在"假成功"，未接的一律如实 `failed/degraded` + 人审
- 真源面：翻译/支付/WhatsApp/社媒/LLM 用真实 Key 接线（列为 B 类，受外部环境约束）
- 门禁：`orchestration_selfcheck.py` **19/19**、能力台账串联 **100%**、`pytest` 全绿、硬锁（登录/角色壳/薄荷主色）**未破**
- 交付求真：所有"已接"必须有实测证据，不凭 Key 存在就报绿

---

## 1. 缺口盘点 A · 代码与数据面（本机可 100% 修）
### A-1 缺失核心表（已核实：models 有无 → 决定修法）
| 表 | model 现状 | 修法 |
|----|-----------|------|
| `purchase_orders` | ❌ model 无 | 新增 model + alembic 迁移 |
| `logistics_shipments` | ❌ model 无 | 新增 model + 迁移 |
| `whatsapp_messages` | ❌ model 无（现有为 im_routing/merchant_profile，非消息表） | 新增 model + 迁移 |
| `experience_records` | ❌ model 无（现有 evolution/eeat 非记录表） | 新增 model + 迁移 |
| `contact_events` | ❌ model 无 | 新增 model + 迁移 |
| `knowledge_bases` | ⚠️ ai_knowledge 有相关 | 复用 model，迁移落地 |
| `invoices` | ✅ invoice_application 有 model | **仅需 alembic 迁移落地 PG** |
| `payments` | ⚠️ 需核对是否独立 model | 迁移/新增 |
| `pipelines` | ⚠️ trace.py 语义待定 | 核对后迁移或新建 |
> 行动：先跑 `alembic revision` 生成全量迁移，把"有 model 未落库"的表全部落地；再为完全缺失的表新增 model。**复跑探表脚本**（probe_emptytables）验收：这些表必须出现在 PG 且可写。

### A-2 空转/降级执行器硬化（按 mock 痕迹排序）
| 执行器 | 问题 | 补法 |
|--------|------|------|
| `publish_executor`(31) | 外发尽是降级/未配置 | 接真实出站渠道（见 B）或硬化为显式 blocked |
| `goodjob_crm_executor`(13) | 依赖的履约表缺失 → 落库后回归 | 依赖 A-1 建表后补写路径 |
| `growth_probe_executor`(12) | 渠道/沙箱红标 | 接真实探活脚本/沙箱 |
| `content_deep_executor`(11) | seo_meta degraded 如实 | 接真 SEO 源或保留如实 |
| `trade_ops_executor`(5) | pi_precheck/goodjob_pi 未配置诚实失败 | 接真引擎(B) 后启用 |
> 判定：mock 降级若确无真实调用点 → 消除；唯有真实外部依赖处保留"如实降级"。

### A-3 闭环焊线（莫比乌斯/三轨）
- 新增 `ExperienceProposal`/`HermesTemplatePolicy`/`ExperienceBizOutcome` 三类 + `resolve_runtime_skill`，接通"业务结果→经验→反馈"闭环（当前四根焊线缺失）
- Wallet Guard → token_ledger 硬拦（现在影子计量）
- 对账巡检、模板权重反哺真实现

## 2. 缺口盘点 B · 真实外部资源接线（受真实 Key/环境约束）
主理人已持有以下环境，作为 P2 阶段逐项接线（每项"接完必须实测证据"）：
- **LLM/DeepSeek Key** → DSH/DeerFlow/内容走真推理，替换 mock
- **翻译引擎 Key** → replace `translate_service` 桩为真源（去 degraded）
- **支付接入** → payment_impl/wechat_pay 接真沙箱/网关
- **WhatsApp/社媒** → Trade AI / goodjob 触达真接线（含人审闸）
> 任一项缺真实凭证 → 如实保留降级并标注"待 Key"，**不假报绿**。

## 3. 施工序（每步都跑验收）
| 阶段 | 内容 | 验收 |
|------|------|------|
| **P0-1** | 核对 models→PG 差异，跑 alembic，新增缺失 model+迁移，全量落库 | probe 探表：核心表出现且可 INSERT/SELECT |
| **P0-1** | **✅ 已完成（本会话 09-18 续跑实测）**：新增 `app/models/trade_fulfillment.py` 9 表 model + 迁移 `114_p0_core_business_tables`；PG 落库 head=114 | probe 9/9 INSERT/SELECT/DELETE 通过；表数 239→248；selfcheck 19/19；unit **861 passed** |
| **P0-2** | 清空转执行器 + 补服务/API 写路径 | 执行器 mock 显著下降；写接口实测落库 |
| **P0-2** | **✅ 已完成**：37 执行器审计消除 `except 吞异常返 succeeded` 假成功 0 残留；2 处模拟成功改如实降级 | `orchestration_selfcheck` 19/19；单元测试全绿 |
| **P1-1** | 接 LLM/翻译真 Key，去 mock/degraded | DSH/DeerFlow/翻译实测走真源 |
| **P1-1** | **✅ 已完成**：翻译接入 Atria-Dawn-Preview LLM 真源（`settings.TRANSLATE_API_BASE_URL/KEY/MODEL`），新增 5 单测 | `test_translate_service_p1_1.py` 15 项通过 |
| **P1-2** | 支付/WhatsApp/社媒接真环境 + 人审闸 | 触发实测；无假成功 |
| **P2-1** | 莫比乌斯焊线 + Wallet 硬拦 + 对账 | 单测/自检绿 |
| **P2-1** | **✅ 已完成**：Wallet Guard 硬拦（`enforce_wallet_gate`、ACQ_HARD_BLOCK_TOKEN）、对账巡检（`billing_reconcile_patrol` beat）、莫比乌斯体验闭环复核 12 项 | wallet:10 / 对账:33 / experience:12 全通过 |
| **P2-2** | 生产闸门全绿 + 全量回归 + 文档更新 | 19/19、pytest（unit）全绿、串联 100% |
| **P2-2** | **✅ 已完成（本会话 09-18 实测）**：编排自检 **19/19**；后端单元测试 **861 passed**；前端 vitest **109 passed**、typecheck、lint、cert:gate **全部全绿**（ready=True）；硬锁未破 | 见下方"完成证据汇总" |

## 4. 铁律承诺
- 不破：LOGIN-LOCK / ROLE-SHELL / DESIGN-TOKEN(薄荷 `#4a9b8c`) / SYSTEM-LOCK-02
- 后端跑法只用启动脚本（禁手敲 uvicorn，防 `REDIS_ENABLED=false` 全站转圈）
- 只读审计/归档不动；备份不进代码树
- 每步留痕到 `.workbuddy/memory/` 当日日志

## 5. 需要你开工前确认的 1 项
P2 阶段接真实支付/WhatsApp/社媒需要**真实沙箱凭证/账号**（生产前建议先用沙箱）。你已确认"有这些接入"，请在工作区一个安全位置提供（或告诉我如何读取），我会**只用于接线、不写入代码库**（确保 .gitignore）。

---

## 完成证据汇总（09-18 起 · 09-19 原生直驱同步）

| 项 | 实测结果 | 证据 |
|----|---------|------|
| 编排自检门禁 | **19/19 PASS** | 09-19 收口复跑 |
| 后端单元测试 | **866 → 912+ passed**（能力域原生+契约） | `pytest tests/unit -q` |
| 全量 pytest（09-19 另线） | **955 passed / 1 skipped**（generate-pi 卡死已修） | `.workbuddy/memory/2026-09-19.md` |
| 设计硬锁 | **未破** | login/role-shell/薄荷主色 |
| P0-1 核心表 | **9/9 可写** · alembic head=114 | trade_fulfillment 九表 |
| **09-19 原生直驱** | **外挂桥已删 · Hermes 直驱 CRM/拓客** | native_* + native_crm_executor |

### 未完成 / 待后续（如实标注）
- **P1-2 生产凭证**：支付沙箱商户号 / WhatsApp Business Token 仍待提供后才能真发；当前「未配置=诚实失败+落库」（**与调度桥无关**）。
- **~~GoodJob 外桥进程~~**：**已废除调度依赖**；CRM 走优丁原生，无需 5188/4188 作 Hermes 前置。
- **lint 遗留警告**：`no-explicit-any` 以 warn 可见，不阻断门禁。
- **quotes.inquiry_id 非空**：谈单在无 inquiry_id 时 DB 双写会降级 memory（既有约束）。
- **P1 业务深融**：单证模板/Prospect/技能数据在原生服务上加厚（不再加回 HTTP 桥）。

### P0-1 交付物（09-18）
- `backend/app/models/trade_fulfillment.py` — 9 表 ORM
- `backend/app/models/__init__.py` — 注册导出
- `backend/alembic_migrations/versions/114_p0_core_business_tables.py` — 建表迁移
- `backend/app/services/trade_fulfillment_store.py` — 九表写路径
- 实测：`orchestration_selfcheck` **19/19**；`pytest tests/unit -q` **861 passed**

### A-2 写路径 + integration 卡死收口（09-18 续跑 · 全部真实开发）

| 项 | 实测结果 | 证据 |
|----|---------|------|
| 业务写路径焊线 | WhatsApp/物流/经验/触点/PI/业务收款/采购单/管线/知识库 已接到新 9 表 | `trade_fulfillment_store.py` + `whatsapp_prospect_service` / `trade_ops_executor` / `experience_engine` / `create_public_lead` / `build_proforma_invoice` / `order_payment_sync` / `order_executor` / `orders` 单证 |
| **integration 卡死根因** | **`PromptInjectionMiddleware` 抽干 ASGI `receive` 后未回放 body** → 带 Body 的 POST（`generate-pi`）永久挂起 | `prompt_injection_middleware.py` 恒 `_replay_receive` |
| 卡死修复后 | `test_full_chain_saas_export` **8/8**；全量 integration **55 passed** | `pytest tests/integration --timeout=40` |
| MOQ 让步语义 | `max(requested, moq_bonus)`（取更优不双计） | Round-2 4% 回到自动授权 |
| purge | 先清 P0-1 九表再清 orders | `scripts/purge_test_data.py` |
| 全量回归 | unit **866 passed** · integration **55 passed** · selfcheck **19/19** | 本会话实测 |

### P1-2 真源核验（求真不假报）
| 资源 | 现状 | 处理 |
|------|------|------|
| LLM/翻译 | `.env` 已接 `AI_OPENAI_*` + `TRANSLATE_API_*` | P1-1 已收口 |
| ~~GoodJob 外桥~~ | **已废除**：HTTP/uj-bridge 脚本已删除 | Hermes **原生直驱** `native_fulfillment`（优丁 PG） |
| WhatsApp | Token/PhoneID **未配置** | 原生路径 `failed` + 落库，不假成功 |
| 支付/社媒外挖 | 无生产商户密钥 / 外挖引擎 | 诚实 failed；业务收款写 `payments` |

### 09-19 同步：TradeAI / goodjob_crm 原生直驱（去桥接）

| 项 | 实测结果 | 证据 |
|----|---------|------|
| 裁定 | goodjob_crm=**本项目 CRM**；TradeAI=**本项目拓客** | 主理人 09-19；`annex_work_mode` 功能域无特权 |
| 原生服务层 | `services/goodjob/native_fulfillment.py` · `services/tradeai/native_acquisition.py` | 单证/履约/线索/商机 + 检索/触达/分类 → 优丁 PG |
| Hermes 执行器 | `goodjob_crm_executor` / `trade_ai_agent_executor` **仅原生** | 无 `GOODJOB_USE_EXTERNAL*` / `TRADEAI_USE_EXTERNAL*` |
| EXECUTOR 插槽 | `orchestration/executors/native_crm_executor.py` | 进程内 submit/poll；`GoodJobExecutor` 仅兼容别名 |
| **已删除桥接** | `trade_document_bridge.py` · `customer_pool_projection.py` · `orchestration/executors/goodjob_executor.py` · `tests/goodjob_bridge_verify.*` · `scripts/connect_external_bridges.py` | 外挂 HTTP/uj-bridge 业务调度链 |
| 环境 | `.env`/`config/dev/.env` 去掉 `GOODJOB_BASE_URL`、`TRADEAI_BASE_URL` | `.env.example` 同步 |
| 性能 | 执行器/trade_ops 优先 `context.db`，热路径少开 SessionLocal | 代码对齐 |
| 门禁（09-19 收口） | unit **912 passed** · 契约/原生相关 25 passed · selfcheck **19/19** | `pytest tests/unit` + `orchestration_selfcheck` |
| 保留（非外挂） | Hermes 系统内任务桥（paperclip/deerflow/hermes_task/orchestrator 等） | 调度总线内部件，未删 |

*本机可做的真实开发已收口；剩余仅为外部支付/WA 等生产凭证（非调度桥）。*

---

*批准后从 P0-1（建表落库）动工。每一阶段完成即向你汇报实测证据。*