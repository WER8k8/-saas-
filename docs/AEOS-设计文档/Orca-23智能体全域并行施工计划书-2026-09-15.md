# Orca 23 智能体全域并行施工计划书 · 后端与数据工业级冲刺总案

> **制定机构**：优丁 YouDing 首席架构师工程部 × Orca 王朝编排中枢  
> **执行平台**：Orca 1.4.201 CLI Automations（23 智能体分布式并行矩阵）  
> **制定时间**：2026-09-15 08:36:00  
> **重大裁定**：**彻底剔除前端 UI/UX 改造任务（前端已完成收敛），23 智能体全量聚焦后端内核、7大模板驱动、单测清零、获客凭证、7步单证引擎、空表注水与全域门禁**。  
> **核心原则**：**物理边界绝缘（零代码冲突）· 先探后写 · 交付求真 · 门禁一票否决**。

---

## 0. 总体战役拓扑与并发矩阵（23 Agent 分布）

为实现 23 个智能体全速并发且**互不产生 Git 冲突、互不抢占同一文件**，全系统划分为 4 大独立作战兵团，每个智能体拥有排他的文件落点与验收标准：

```
                    ┌─────────────────────────────────────────────────────────┐
                    │               皇帝 / 架构师总控监工中枢                  │
                    └────────────────────────────┬────────────────────────────┘
                                                 │
      ┌──────────────────┬───────────────────────┴──────────────┬──────────────────┐
      ▼                  ▼                                      ▼                  ▼
【第一兵团：核心调度】  【第二兵团：获客凭证】                【第三兵团：外贸履约】  【第四兵团：数据门禁】
Agent 01 - 06        Agent 07 - 12                          Agent 13 - 18          Agent 19 - 23
(Hermes/7模板/单测)  (PC凭证/GEO矩阵/分发)                  (GoodJob/7步单证/翻译) (注水工程/Worktree清理)
```

---

## 第一兵团：Hermes 核心调度与 7 大 L1 模板图引擎（6 智能体）

### 第 1.1 节 · Agent-01（Codex）：补齐 7 大 L1 TaskGraph 模板
- **任务代号**：`ORCA-B1-01`
- **文件独占边界**：`backend/app/services/hermes/planner_service.py`
- **核心目标**：
  在 `planner_service.py` 中导出并实现 7 个 TaskGraph 模板函数，挂入 `_TEMPLATES` 匹配：
  1. `_fulfillment_graph`：严格 8 节点（`inquiry.capture` ➔ `order.create` ➔ `document.generate_pi` ➔ `billing.meter` ➔ `crm.sync_stage` ➔ `document.generate_trade_docs` ➔ `logistics.track` ➔ `billing.meter`）；
  2. `_social_outreach_graph`：3 节点（`prospect.scrape` ➔ `outreach.whatsapp` ➔ `inbox.classify`），含 `approval_required=["outreach.whatsapp"]`；
  3. `_product_launch_graph`：4 节点（`product.create` ➔ `media.render` ➔ `seo.optimize` ➔ `engagement.send`）；
  4. `_research_analysis_graph`：3 节点（`research.deep_run` + `wangcai.ask` ➔ `ai.reason`）；
  5. `_lead_generation_graph`：3 节点（`lead.search` ➔ `browser.scrape` ➔ `billing.meter`）；
  6. `_browser_evidence_graph`：2 节点（`browser.scrape` ➔ `forum.post`）；
  7. `_ubrain_assistant_graph`：2 节点（`ubrain.chat` ➔ `ai.chat`）。
- **验证命令**：
  ```powershell
  python 上线网站.worktrees\agents-install-vscode-cline-deploy-strix\backend\scripts\verify_hermes_executor_coverage.py
  # 必须退出码 0，24 个执行器 100% 被驱动
  ```

### 第 1.2 节 · Agent-02（Codex）：清零 23 个测试用例
- **任务代号**：`ORCA-B1-02`
- **文件独占边界**：`backend/tests/unit/test_planner_fulfillment.py` 与 `test_planner_undriven_drivers.py`
- **核心目标**：
  配合 Agent-01 补齐的模板，对齐单测断言结构与 Mock 上下文，彻底解决 23 个 FAILED 测试项。
- **验证命令**：
  ```powershell
  cd 上线网站.worktrees\agents-install-vscode-cline-deploy-strix\backend
  .\venv\Scripts\python.exe -m pytest tests/unit/test_planner_fulfillment.py tests/unit/test_planner_undriven_drivers.py -v
  # 必须 23/23 全绿
  ```

### 第 1.3 节 · Agent-03（Claude）：执行器 100% 覆盖率断言加固
- **任务代号**：`ORCA-B1-03`
- **文件独占边界**：`backend/scripts/verify_hermes_executor_coverage.py`
- **核心目标**：
  消除 `ImportError: cannot import name '_fulfillment_graph'`，完善全系统 24 个执行器的拓扑闭环检测脚本，将其固化为 CI 必经门禁。
- **验证命令**：`python backend/scripts/verify_hermes_executor_coverage.py` 返回 0。

### 第 1.4 节 · Agent-04（Claude）：修复 `model_call_ledger` UUID 语法暗病
- **任务代号**：`ORCA-B1-04`
- **文件独占边界**：`backend/app/services/model_gateway/ledger.py`
- **核心目标**：
  在向 `model_call_ledger` 插入数据前增加防御性校验：若 `tenant_id` 不符合 UUID4/5 格式（如测试桩传入 `"t1"`），使用 `uuid.uuid5(uuid.NAMESPACE_DNS, str(tenant_id))` 确定性转换或置空，杜绝 PostgreSQL 抛出 `invalid input syntax for type uuid: "t1"`。
- **验证命令**：运行相关计费测试无语法报错。

### 第 1.5 节 · Agent-05（Grok）：多租户非确定性上下文定序修复
- **任务代号**：`ORCA-B1-05`
- **文件独占边界**：`backend/app/services/tenant_scenario_service.py`
- **核心目标**：
  在 `resolve_tenant_id_for_user` 中，将无定序的 `.first()` 改造为带有严格创建时间倒序与活跃状态过滤的确定性查询：
  ```python
  link = db.query(UserTenant).filter(UserTenant.user_id == user.id, UserTenant.is_active.is_(True)).order_by(UserTenant.created_at.desc()).first()
  ```
- **验证命令**：多租户场景单测通过。

### 第 1.6 节 · Agent-06（Gemini）：社媒凭证 Fernet 密文加密加固
- **任务代号**：`ORCA-B1-06`
- **文件独占边界**：`backend/app/core/encryption.py` 与 `backend/config/dev/.env`
- **核心目标**：
  落地 `CREDENTIAL_ENCRYPTION_KEY` 密文加解密，海外社媒账号密码、WhatsApp Session Token 写入数据库前必须经过对称加密，杜绝明文泄露。
- **验证命令**：检查落库字段为密文字符串。

---

## 第二兵团：出海获客凭证、GEO 排名矩阵与分发引擎（6 智能体）

### 第 2.1 节 · Agent-07（Codex）：【PC-01】前端 API 层凭证丢包修复
- **任务代号**：`ORCA-B2-01`
- **文件独占边界**：`frontend/admin/src/api/index.ts`（L430–L460，纯 API 客户端层）
- **核心目标**：
  重构 `createPlatformAccount` 与 `updatePlatformAccount` 契约：将此前遗漏的 `cookie_data`, `token_data`, `configs`, `alibaba_member_id` 等平铺字段完整打包进 POST/PUT Payload，使前端 API 调用时凭证真实入库。
- **验证命令**：`npm run typecheck` 0 TS 错误。

### 第 2.2 节 · Agent-08（Claude）：【PC-03】补齐 21 个出海平台 GEO 排名档案
- **任务代号**：`ORCA-B2-02`
- **文件独占边界**：`backend/app/services/geo/platform_rank_registry.py`
- **核心目标**：
  在注册表中补齐缺失的 21 个海外核心获客平台档案：AliExpress, Amazon Seller, Blogger, Kompass, LINE Official, Lazada, Pinterest, Quora, Shopee, Snapchat, Telegram Channel, Thomasnet, TradeKey, Tumblr, VK, WhatsApp, WordPress.com, Zalo, eBay, 淘宝特价, 脉脉。赋予其合理的 `geo_weight`（0.85~0.95）与 `rank_priority`（1~10），消除出海发布漏排降权。
- **验证命令**：运行 `exploration_gap_report()` 确认 `unregistered_in_rank_registry` 降至 0。

### 第 2.3 节 · Agent-09（Grok）：【PC-04】Cookie 会话失效自动巡检 Beat
- **任务代号**：`ORCA-B2-03`
- **文件独占边界**：`backend/app/tasks/credential_health_tasks.py`
- **核心目标**：
  新增 Celery Beat 定时任务（每 6 小时执行一次），轻量探测各大社媒/平台 Cookie 会话是否过期；若探测失败或返回 401，自动将 `platform_accounts.login_status` 置为 `expired` 并触发租户站内通知。
- **验证命令**：`python backend/scripts/orchestration_selfcheck.py`（19/19 全绿）。

### 第 2.4 节 · Agent-10（Gemini）：【PC-05】出海社媒文本发布适配器补齐
- **任务代号**：`ORCA-B2-04`
- **文件独占边界**：`backend/app/services/publish/adapters/`
- **核心目标**：
  为 Pinterest, Tumblr, Reddit, Telegram 补齐轻量标准化发布适配器，实现自动带出外贸独立站链接与 `#BuildingMaterials` 标签。
- **验证命令**：单测覆盖率提高，无空指针异常。

### 第 2.5 节 · Agent-11（Goose）：【P0-04】下线 Stub SEO 伪数据
- **任务代号**：`ORCA-B2-05`
- **文件独占边界**：`backend/app/api/v1/routes/seo.py`
- **核心目标**：
  下线旧版硬编码的 150 假关键词 Stub，全面桥接到真正的 `KeywordService` 与数据库真实 `keywords` 表。
- **验证命令**：API 返回的数据与真实数据库 keywords 表完全一致。

### 第 2.6 节 · Agent-12（Cline）：【P0-01/P0-03】裂变与高级 SEO 路由挂载
- **任务代号**：`ORCA-B2-06`
- **文件独占边界**：`backend/app/api/v1/api.py`
- **核心目标**：
  挂载遗留的 `referral_router`（用户裂变）以及 `app/api/v1/seo/*` 72 个高级 SEO 路由端点。
- **验证命令**：路由探测脚本核实路由总数由 146 扩展并全部返回有效状态。

---

## 第三兵团：GoodJob CRM 外贸 7 步履约与单证套打中枢（6 智能体）

### 第 3.1 节 · Agent-13（Codex）：形式发票 (PI) 极速生成套打中枢
- **任务代号**：`ORCA-B3-01`
- **文件独占边界**：`backend/app/services/commercial/proforma_invoice_service.py`
- **核心目标**：
  实现标准国际商会（ICC）格式的英文 Proforma Invoice 一键导出（PDF/HTML/Print）。支持注入建材 22 参数、FOB/CIF 离岸条款、起运港/目的港、国际外汇收款账户（Beneficiary Bank & SWIFT Code）。
- **验证命令**：调用生成接口，验证生成的 PI 格式合规且参数完整。

### 第 3.2 节 · Agent-14（Claude）：商业发票 (CI) 与装箱单 (PL) 自动平衡精算
- **任务代号**：`ORCA-B3-02`
- **文件独占边界**：`backend/app/services/commercial/packing_list_service.py`
- **核心目标**：
  实现 Commercial Invoice 与 Packing List 智能生成。基于商品立方数（CBM）与毛重净重自动计算 20GP / 40HQ 集装箱配载率，一键套打出关单证。
- **验证命令**：单元测试验证箱单件数、毛重、体积与报关单严格闭合。

### 第 3.3 节 · Agent-15（Grok）：WhatsApp 双向实时翻译与百种性格博弈
- **任务代号**：`ORCA-B3-03`
- **文件独占边界**：`backend/app/services/chat/whatsapp_translator_service.py`
- **核心目标**：
  打通 WhatsApp 消息双向实时翻译（阿拉伯语、西语、俄语、德语 ➔ 中文），结合 100 种买家性格特征库自动生成外贸专业还盘话术。
- **验证命令**：输入阿拉伯语询价，验证返回中文翻译与英文报价草稿。

### 第 3.4 节 · Agent-16（Gemini）：国际海运提单轨迹追踪服务
- **任务代号**：`ORCA-B3-04`
- **文件独占边界**：`backend/app/services/logistics/tracking_service.py`
- **核心目标**：
  对接真实海运提单号（B/L No.）轨迹查询接口，根据提单号获取订舱、装箱、离港、在途、到港各节点状态并持久化。
- **验证命令**：查询测试提单 `BL-100000` 正常返回完整海运时间线。

### 第 3.5 节 · Agent-17（Goose）：外部子系统熔断与诚实 Failed 机制加固
- **任务代号**：`ORCA-B3-05`
- **文件独占边界**：`backend/app/services/external_subsystems/base.py`
- **核心目标**：
  加固 TradeAI@8010 与 GoodJob@5188 通信底座：严禁假调用，当子系统服务未启动或缺少 API Key 时，严格返回 `is_mock=True` 与 `status=failed (honest)`，绝不伪造虚假成功响应。
- **验证命令**：`_tmp_verify_external_subs.py` 9/9 PASS。

### 第 3.6 节 · Agent-18（Aider）：外贸 7 步履约状态机持久化
- **任务代号**：`ORCA-B3-06`
- **文件独占边界**：`backend/app/services/fulfillment/fulfillment_state_machine.py`
- **核心目标**：
  建立统一履约状态机：询盘捕获 ➔ 需求核算 ➔ 形式发票 (PI) ➔ 定金核销 ➔ 生产跟单 ➔ 发运单证 ➔ 尾款物流，状态跃迁时自动触发 Webhook 与审计留痕。
- **验证命令**：履约全流程单元测试通过。

---

## 第四兵团：空表注水工程、产业带数据与门禁哨兵（5 智能体）

### 第 4.1 节 · Agent-19（Codex）：海关报关与提单台账注水
- **任务代号**：`ORCA-B4-01`
- **文件独占边界**：`backend/scripts/seeds/seed_customs_ocean.py`
- **核心目标**：
  向 `bill_of_ladings`（海运提单）注入 50 条标杆提单、`customs_clearance_records`（清关单）注入 30 条数据，关联中东（JEDDAH/HAMAD）与欧美港口。
- **验证命令**：`python tools/db_probe.py --density` 验证两表行数。

### 第 4.2 节 · Agent-20（Claude）：建材产业带与商户目录注水
- **任务代号**：`ORCA-B4-02`
- **文件独占边界**：`backend/scripts/seeds/seed_industry_clusters.py`
- **核心目标**：
  向 `tenant_product_candidates`（产业带候选库）注入 100 款大城/河间绝热保温标杆品类、佛山陶瓷卫浴与山东钢结构标准化 SKU（附带 22 参数）。
- **验证命令**：检查产业带品类候选接口正常渲染。

### 第 4.3 节 · Agent-21（Grok）：出海社媒养号与矩阵轨迹注水
- **任务代号**：`ORCA-B4-03`
- **文件独占边界**：`backend/scripts/seeds/seed_nurture_traces.py`
- **核心目标**：
  向 `social_account_nurture_traces`（养号轨迹）注入 100 条合规行为记录（浏览、点赞、外贸询盘互动日志），激活冷启动账号信誉度。
- **验证命令**：`python tools/db_probe.py --density` 查验。

### 第 4.4 节 · Agent-22（Gemini）：国际白标域名与 DNS 记录注水
- **任务代号**：`ORCA-B4-04`
- **文件独占边界**：`backend/scripts/seeds/seed_white_label_domains.py`
- **核心目标**：
  向 `tenant_white_label_domains` 注入 20 条标准外贸独立域名台账，包含 CNAME 指向、Let's Encrypt 证书有效天数与 SEO 权重状态。
- **验证命令**：查验表记录数 20。

### 第 4.5 节 · Agent-23（QA-Commander）：常驻门禁哨兵与历史 Worktree 清理
- **任务代号**：`ORCA-B4-05`
- **文件独占边界**：根目录与 `scripts/`
- **核心目标**：
  1. 自动化清理 18 个历史 `auto-t-*` run worktree 残余目录（`git worktree prune`），释放磁盘空间；
  2. 常驻循环自检：每 15 分钟跑一遍 `orchestration_selfcheck.py` 与 `cert:gate`，任何智能体提交导致门禁变红立即回滚阻断并告警。
- **验证命令**：`git worktree list` 干净整洁，仅保留活跃工作树；19/19 门禁长绿。

---

## 5. Orca Automations 派单配置清单（一键投产）

各智能体可直接在 Orca 中配置以下自动化任务元数据（Provider 统一推荐 `codex`，无人值守零弹窗卡顿）：

| 派单 ID | 兵团 | 任务描述 | 核心操作文件 | 触发周期 |
|---|---|---|---|:---:|
| `b1-templates` | 一兵团 | 补齐 7 大 TaskGraph 模板与覆盖率 | `planner_service.py` | 每小时 / 触发 |
| `b1-pytest`    | 一兵团 | 清零 23 个测试 FAILED 项 | `tests/unit/test_planner_*` | 每小时 / 触发 |
| `b2-cred`      | 二兵团 | PC-01 凭证字段丢包修复 | `api/index.ts` | 单次并发 |
| `b2-geo`       | 二兵团 | PC-03 21 平台 GEO 注册表补全 | `platform_rank_registry.py` | 单次并发 |
| `b3-pi`        | 三兵团 | 形式发票 (PI) 套打中枢 | `proforma_invoice_service.py` | 单次并发 |
| `b3-packing`   | 三兵团 | 商业发票与装箱单平衡引擎 | `packing_list_service.py` | 单次并发 |
| `b4-seeds`     | 四兵团 | 空表批量注水提速至 60+ 张 | `backend/scripts/seeds/` | 每小时 / 触发 |
| `b4-sentinel`  | 四兵团 | Worktree 清盘与全域门禁哨兵 | 根目录维护 | 每日常驻 |

---
*本计划书已通过架构部终审，全量聚焦后端与核心引擎，各智能体即刻按独立分支并行施工！*
