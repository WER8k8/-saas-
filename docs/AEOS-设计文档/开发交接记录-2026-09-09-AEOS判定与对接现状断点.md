# 开发交接记录 · AEOS 判定与 TradeAI×CRM 对接现状（2026-09-09 断点）

> 接续：`docs/开发交接记录-n8n接通与PostgreSQL切换_2026-09-08断点.md` 之后。
> 本会话主题：**多模型设计统一判定 + 350+ 资产"调色盘"可行性核验 + Trade AI Agent × GoodJob CRM 对接现状判勘**。

---

## 一、本会话沉淀的权威文档（新产出 2 份）

| 文件 | 内容 |
|---|---|
| `docs/多模型设计统一裁定书-2026-09-09.md` | **三级判定链**：T0 事实层(端口/库/真源，违反即错) → T1 硬锁(AGENTS.md §3，破锁即错) → T2 愿景(只标 🟢已实现/🟡已规划/🔴待接入)。含判例表、个体交用的事实声明自证表、仲裁流程。解决"每个大模型设计都不一样"的裁决尺。 |
| `docs/TradeAI-GoodJob-Hermes总线对接方案-2026-09-09.md` | 让 `_external/trade-ai-agent` 与 `_external/goodjob-crm` 真正接入 Hermes 总线的详细方案（现状判据 + 6 件套 P0–P2 落地方案）。 |

## 二、关键实测结论（覆盖旧"表面通过"口径）

1. **双 executor = 纯 mock 桩**：`backend/app/services/hermes/executors/trade_ai_agent_executor.py`、`goodjob_crm_executor.py` 均返回假数据、不真调 `_external`。此前 walkthrough「4 节点全绿 + WhatsApp 触达」是**桩空跑**。
2. **`_external` 两子模块是真实体**：
   - `trade-ai-agent`：FastAPI，13 router(`/api/v1/*`)，自带 PG `trade_ai`@5432/Redis/Celery；⚠️自身占 8000，与主后端冲突，接入须错端口。
   - `goodjob-crm`：Node/TS，自带 `uj-bridge-routes.ts`+`agent-api-contracts.ts` 官方桥接、`whatsapp-service.ts`(whatsapp-web.js+Twilio 双通道)、单证工作室、`customs-export.ts`；依赖 MySQL(`CRM_STORE=mysql`)+BullMQ(Redis)。
3. **自检项数口径漂移**：索引 14/14 vs walkthrough 19/19 vs implementation_plan 18 → **以实跑 `orchestration_selfcheck.py` 为唯一真值**。

## 三、上一会话涉及、本会话复核的路径漂移修正

- `meticulous_scan_catalog.json`(2.18MB)、`meticulous_frontend_assets_catalog.json`(627KB) 实际均在 **工作区 `scratch/`**，不在 brain/scratch。
- `clean_cached_conversations/` 在 **brain/scratch/** 下。
- Obsidian Junction **三扇直通已实测成立**：`01→docs`、`02→.workbuddy/memory`、`03→00-文档中心`，`my-obsidian-notes` 同指向。两者双向实时同步。

## 四、当前运行真相（与强制索引一致，未变）

- 后端 = 原生 PG15.8@5433（229 表，head=105）+ Redis@6379 + n8n@5678。
- 3000 官网真源 = `主要备份\上线网站\frontend`（Vite 暖米白 `#f5f4f0` + 砖橙 `rgb(154,52,18)`），worktree Nuxt 薄荷旧版**禁占 3000**。
- Admin 5174（薄荷 `#4a9b8c`）、SEO 后台 5173 / SEO 后端 3001。

## 五、下一步建议（对接推进）

1. **P0 封真桥**：新增 `clients/trade_ai_client.py` + `clients/goodjob_client.py`，改造两 executor 由桩→真调；trade-ai 错开端口(8010)。双目录（worktree + `主要备份`）同步铁律。
2. **P1**：Saga 真补偿(删孤儿单证+退token)、真 Token 计量、WhatsApp `wa_message_id` 回写。
3. **P2**：升级自检 Check 14/15/16 为**真实端口冒烟+副作用断言**；统一自检项数口径；重写集成测试为真执行版。

> 详细步骤见 `docs/TradeAI-GoodJob-Hermes总线对接方案-2026-09-09.md`，未动工前不改业务代码。

---

*EOF · 交接记录 2026-09-09 · 由本会话源码实测沉淀*