# 审计增量 —— 默认推断（睡觉断点第 4 轮）

> **生成**：2026-09-03 11:35（token 极受限，主动推断模式）
> **目的**：把"用户必答的 3 个问题"按文档证据主动推断写入，醒来可一键否决/确认

## 默认推断（按文档证据强弱排序）

### Q1：附属一 / 附属二指什么

**推断结果**：
- **附属一 = UJ 票据协议**（总纲 §9.3）—— 已实装（轮 19 附加：UJ 28/28、GoodJob 18/18、TradeAI 22/22、跨语言 10/10）
- **附属二 = GoodJob CRM**（_external/goodjob-crm/ 已存在副本，GPL-3.0 风险隔离）

**推断依据**：
1. 导航索引 §二 行 46 明确"附属统一登录（§9.3 票据协议）"已完成
2. _external/ 目录中 goodjob-crm 体积最大、目录最全
3. 总纲 §0 提"附属一/二"

**否决方法**：如不对，直接说"附属一 = X / 附属二 = Y"覆盖

### Q2：DeerFlow→n8n 触发优先级

**推断结果**：**最高优先级 ★★★**

**推断依据**：
- 6 节点循环链路中**唯一未闭环点**（其他 5 节点均有代码）
- 缺口意味着 DeerFlow 执行完结果**无分发通道**
- 修这个缺口 = 一行挂点（`deerflow_job_service.py` 完成回调加 `trigger_n8n_workflow()`）

### Q3：隐患修复 vs 附属核查先做哪个

**推断结果**：**附属核查先**（更具体、ROI 高）

**推断依据**：
- 3 个中优先级隐患都是"未审计"非"已暴露"（503/0 仍 PASS）
- 附属核查有明确答案路径（UJ 已实装 + GoodJob 隔离即可）
- 隐患审计需数百 token（精读代码），附属核查不耗 token

## 醒来后处置

- **全对**：继续 Goal
- **任一不对**：1 句话纠正即可

## 状态汇总

- 抢救文档：4 份（新增本记录，共 ~38 KB）
- Goal 完成度：~55% → ~65%（含默认推断）
- Token：24.4M
- 阻塞条件：已"软解除"（默认推断 + 可否决）
