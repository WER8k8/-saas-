# 任务规划总表 · 审校 · 2026-09-10

> **对象**：`docs/任务规划总表-2026-09-10.md`（他人/其他模型生成）
> **审校人**：Claude（上一轮交接方）｜ **结论**：**总表可用，但有 3 处严重错误 + 基线过时，Codex 开工前必须修正，否则会做无用功甚至覆盖真实现。**
> **原则**：以代码实测为准，不以总表表述为准。

---

## 🔴 严重错误（会误导 Codex 做错事）

### ① 段 N 把 4 个已真接入的执行器标成"桩"（最严重）

总表 N.5-N.9 写着：

| 执行器 | 总表标注 | **代码实测（2026-09-10）** |
|---|---|---|
| `nurture` | 桩 | ✅ **真接入** `social_nurture_service`（建周期/推进 + 诚实标注"不代表已养好"） |
| `accio` | 桩 | ✅ **真接入** `ubrain/accio_sales_service`（采购商候选/开发信/谈单） |
| `trade_ai_agent` | 桩 | ✅ **真接入** `services/adapters/tradeai`（真实适配器；未注册 workflow 即明确 failed） |
| `goodjob_crm` | 桩 | ✅ **真接入** `services/goodjob/trade_document_bridge`（真实 HTTP 桥；未配 GOODJOB_BASE_URL 即明确 failed） |
| `deerflow` | 部分 | ✅ **真实现**（9 意图，始终是真实现） |

**后果**：Codex 若照总表，会**重写已经接好的执行器**——甚至可能把真实现覆盖回假数据。
**处置**：开工前先把 N.5-N.8 状态改为「✅ 已完成」；只有一段 B（端到端）跑过后才能确认真接功能在运行时也通。

### ② 段 G.9 与实测矛盾

总表 G.9：「3000 端口跑的是 worktree Nuxt 薄荷版（应为主要备份 Vite 版）」。

**实测（2026-09-10）**：**3000 端口当时未启动**（端口关闭），无任何进程。且运行真相源= `主要备份/上线网站/frontend`（Vite 暖米白+砖橙），`官网启动强制索引-必读.md` 已写明。
**处置**：不要照 G.9 去"改"——先 `Get-NetTCPConnection -LocalPort 3000` 实测当前谁在跑。若确实起了 worktree Nuxt，才需要切。

### ③ G.11 自相矛盾 + 段 N 里执行器数量口径冲突

总表 G.11 说「执行器数量口径不一（17 vs 18+9 vs 22）」，但段 N 又写「9 个已注册执行器」。
**实测**：**9 个**（deerflow / site_builder / content / publish / nurture / egress / accio / trade_ai_agent / goodjob_crm），`tools/verify_executors.py` 已验证。**口径就是 9**，其他数字是旧文档残留。

---

## 🟡 基线过时（总表基于旧文档生成）

- **K.4 自己承认**：文档有 5 处过时表述（`串联率 1.3%` 等）。**实测串联率现在是 15.3%**（`tools/capability_ledger.py`，23 个模块真串联）。
- 总表自称"唯一任务真相源"，但**没引用已就绪的接线方案**，会造成两套口径：
  - `docs/能力台账/归属表.md`（模块该不该串 + 五类标签）
  - `docs/能力串联接线图-2026-09-10.md`（每模块怎么串 + hook/backoffice 处置）
  - `docs/50平台接线台账-2026-09-10.md`（50 平台逐个状态 + 五批顺序）
- **段 I（50 平台）与我的台账重复但没引用它**：请 Codex 合并引用，避免两套清单。

---

## 🟢 可执行（符号已核对为真）

段 C（Phase 2a 首批执行器）依赖的符号**全部真实存在**：

| 能力 | 依赖 | 实测位置 |
|---|---|---|
| `inquiry.capture` | `InquiriesUnifiedService.create_public_lead` | `services/inquiries_unified_service.py:409` ✅ |
| `product.create` | `ProductService.create_product` | `services/product_service.py:171` ✅ |
| `logistics.track` | `fetch_tracking_payload` | `services/logistics_tracking_service.py:24` ✅ |

段 C 可以照做。但**注意段 C 前置条件写的是"段 B 完成"** —— 完全正确，别跳过 B 直接做 C。

---

## ⚠️ 需要 Codex 先核对再动

| 项 | 为什么 |
|---|---|
| **段 A 的 11 项「已完成」** | 是另一个模型做的，我未验证。尤其 A.10（改 `config/dev/.env` 的 REDIS）——我实测**生效层是 `backend/.env`**（覆盖 config/dev/.env），改下层可能无害也可能改错层。开工先抽查。 |
| **B.1 用 `scripts/dev-backend-bootstrap.ps1` 启动** | 该脚本历史上有强制 `REDIS_ENABLED=false` 的问题（A.8/A.9 声称已修）。**交接简报推荐 `start-lan.bat`（LAN 模式）**。两者行为可能不同，先确认再选。 |
| **B.4 用 `--user tenant --password tenant123`** | 该账号存在（我建过），可行。与 `admin/admin123` 并列均可。 |

---

## 📋 给 Codex 的一句订正话术

> 开工前请对 `docs/任务规划总表-2026-09-10.md` 做三处修正：
> ① 段 N：`nurture` / `accio` / `trade_ai_agent` / `goodjob_crm` **已真接入**，不是桩（见我的实作记录）；不要重写。
> ② 段 G.9：先实测 3000 端口当前谁在跑，不要照旧表述直接改。
> ③ 执行器数量 = **9**；串联率 = **15.3%**。
> 然后按总表执行顺序（段 B → 段 C → 段 H.1-H.2 → 段 G → 段 D → 段 I），每轮收尾跑门禁 + 刷新本表。

---

*审校 2026-09-10 · 以代码实测为准。若有冲突，总表不代表现状。*