# 融合项目版本核对 · 2026-09-10

> **问题**：融合进来的这些项目，本地是不是最新版？
> **方法**：本地 `git log` + 上游 GitHub / Gitee API 实测比对（非猜测）。
> **结论**：**9 个里有 3 个明显落后、1 个已是最新、其余各有特殊情况；其中 2 个不能盲目拉取。**

---

## 一、版本对照总表

| # | 项目 | 本地 HEAD / 版本 | 本地日期 | 上游最新 | 上游日期 | 判定 |
|---|---|---|---|---|---|---|
| 1 | **DeerFlow 2.0** | `b002d559` | 2026-09-06 | `0d49253` | 2026-09-09 | 🔴 **落后 3 天** |
| 2 | **ECC** | `e04ea0b` (v2.2.1) | 2026-09-03 | `cc91c24` | 2026-09-09 | 🔴 **落后 6 天** |
| 3 | **agency-agents-zh** | `972452c` (v1.4.0) | 2026-08-26 | `e00aed9` | 2026-09-10 | 🔴 **落后 15 天** |
| 4 | **Trade AI Agent** | `28976d0` | 2026-08-30 | `28976d0` | 2026-08-30 | ✅ **已是最新（commit 完全一致）** |
| 5 | **GoodJob CRM** | `5e66243` (v0.1.0) | 2026-09-06 | — | — | ⚪ **自建固化，无上游可拉** |
| 6 | **n8n** | 容器 v2.37.10 | — | **2.38.5** | — | 🟡 **落后 1 个小版本** |
| 7 | **DeepSeek Harness** | v0.1.3-alpha.2 | 2026-09-08 | 待查（alpha 渠道） | — | ⚪ 未拉 git，vendored 快照 |
| 8 | **MOSS-VL** | 无 `.git` | — | `OpenMOSS/MOSS-VL` | — | ⚪ 未拉 git，目录快照 |
| 9 | **Accio** | 内部代码 | — | — | — | ⚪ **无独立上游仓**（已内化为 `services/ubrain/`） |

---

## 二、⚠️ 拉取前必看：本地是否被改过

**这决定能不能直接 pull——拉错会冲掉融合成果。**

| 项目 | 未提交改动 | 可否直接拉 |
|---|---:|---|
| DeerFlow 2.0 | **0** | ✅ **可以安全拉** |
| ECC | **1**（仅 `INTEGRATION_NOTES.md`，我方新增说明） | ✅ 可以安全拉（是未跟踪文件，不冲突） |
| Trade AI Agent | **0** | ✅ 已最新，无需拉 |
| agency-agents-zh | **8**（改了 `.gitattributes` / `.gitignore` / `LICENSE` 等） | ⚠️ **先备份本地改动再拉** |
| GoodJob CRM | **571** | 🚫 **绝对不要盲拉**（且无上游可拉） |

---

## 三、建议动作

### 需要 Codex 拉取的（3 个）

```bash
# 1. DeerFlow 2.0（落后 3 天，干净，可直接拉）
cd _external/deer-flow && git pull

# 2. ECC（落后 6 天，仅 1 个未跟踪文件，可直接拉）
cd _external/ecc && git pull

# 3. agency-agents-zh（落后 15 天，但本地有 8 处改动 —— 先存后拉）
cd _external/agency-agents-zh
git stash          # 或先 commit 本地改动
git pull
git stash pop
```

### 不需要拉的

- **Trade AI Agent** —— 已与上游一致（`28976d0`）。
- **GoodJob CRM** —— 自建固化包，**无上游**；571 处改动说明它已被本地化。
- **Accio** —— 不是外部仓，已内化进 `services/ubrain/`。
- **MOSS-VL / DeepSeek Harness** —— 是目录快照非 git 仓，要更新只能整包替换（且 Harness 是 alpha 渠道，谨慎）。

### 需要单独升级的

- **n8n**：v2.37.10 → 2.38.5。属容器镜像与工作流兼容性变更，**建议单独评估后再升**（现有 2 个工作流是桩，升版影响面小）。

---

## 四、一句提醒

**版本新 ≠ 融合好。** 这 9 个里，DeerFlow 是真跑的执行器，它落后 3 天值得更新；但真正卡住系统的是 **Accio 是骨架、Trade AI / GoodJob 是 mock** —— 那是"接没接上"的问题，不是"版本新不新"的问题。

**建议顺序：先把已接上的补真，再拉最新源码。** 否则拉完最新，仍是一片 mock。

---

*本核对为 2026-09-10 实测，上游数据来自 GitHub / Gitee / npm registry 实时查询。*
