# 给 TRAE 的盘点指令 · 代码像素级全面盘点

> **发起方**：Claude（编排链交接方，bash 通道故障中）｜ **执行方**：TRAE
> **目的**：对优丁项目做一次「像素级」全面代码盘点，产出**与既有工具口径一致**的实数。
> **为什么特别提醒**：此前有一个模型基于**旧文档**生成了任务总表，把已真接入的执行器标成"桩"、串联率写成旧值 1.3%（实为 15.3%）。**本次盘点必须以「运行工具 + 读源码」为准，不以任何文档为准。**

---

## 一、先读这些（建立正确基线，按序）

| 文件 | 作用 |
|---|---|
| `AGENTS.md`（工作区根） | 硬锁 + 文档地图 |
| `docs/00-会话产出串联索引-2026-09-10.md` | 本会话脉络 + **哪些旧结论被推翻** |
| `docs/编排主链打通实作记录-2026-09-10.md` | 已交付代码 + 去假专项记录 |
| `docs/任务规划总表-审校-2026-09-10.md` | 总表 3 处严重错误的订正 |

---

## 二、用这些脚本产出实数（可重跑、口径统一）

> 在**工作区根**运行。这些是我交接时建好的工具，**口径以它们为准**。

| 工具 | 产出 | 你应该检查的事 |
|---|---|---|
| `python3 tools/code_atlas_gen.py --out docs/代码地图`<br>`python3 tools/code_atlas_md.py` | 逐文件地图（新执行器加入后需重生成） | 执行器是否都在地图里 |
| `python3 tools/verify_executors.py` | 执行器契约 + 能力声明 + 冲突报告 | **应为 9 个执行器 / 9 合规** |
| `python3 tools/capability_ledger.py` | 能力台账 + 串联率 | **应为 ≥15.3%**（我交接时 15.3%） |
| `python3 tools/capability_tags.py` | 归属标签 | **未归类必须为 0** |
| `python3 tools/fake_scan.py` | 假交付扫描 | **HIGH 必须 0** |
| `python3 tools/smoke_orchestration.py --live` | **端到端冒烟（需要服务已启动）** | **Phase 2 核心**，全绿才是真通 |
| `python3 tools/db_probe.py` | 数据库（表数/RLS/种子） | 对比 09-09 快照 `docs/数据库实测快照-2026-09-10.md` |

**启动服务优先**：`start-lan.bat`（LAN 模式，后端 0.0.0.0:8001）。冒烟 `--live` 依赖它。

---

## 三、特别注意（别犯前人的错）

1. **不要相信任何 Markdown 里的数字**，一切以工具输出/源码为准。
2. **9 个执行器已全部真接入**（site_builder/content/publish/nurture/egress/accio/deerflow/trade_ai_agent/goodjob_crm）——**都不是桩**。goodjob/trade_ai 是「真桥但未配置钥匙」，未配置时**明确 failed**，不等于桩。
3. **串联率**（`capability_ledger.py`）是最客观的「接了多少」指标。
4. **你发现的任何新问题**：记下来给 Claude 做设计（编号 + 文件 + 现象 + 建议方向）。

---

## 四、输出格式（给 Claude 做设计的输入）

```
## TRAE 盘点输出
1. 执行器：N 个 / 契约合规 N / 能力宏 N
2. 串联率：X%（对比交接 15.3%）
3. 数据：表数/RLS/种子 与快照对比
4. 端到端冒烟：通过/失败（失败原因）
5. 新发现问题：编号 文件 现象 建议
6. 代码地图：是否重生成
```

---

*本指令由 Claude 于 2026-09-10 提供，供 TRAE 执行；输出将作为下轮设计方案的输入。*