# P0-3 · 空表收口 ≤20 决策 · 2026-09-12

> **本文件作用**：把 207 张空表收口为可决策矩阵，作为 P0-3 退出标准。
> 配套脚本：`tools/empty_table_triage.py`（可重跑）
> 数据基线：`PG 15.8 @ 127.0.0.1:5433 / youding_dev`（234 表 / 27 有数据 / 207 空）

---

## 一、一句话结论

**「≤20 保留」的真实口径**：把 207 张空表中 **真正无业务归属、无 ORM 模型的残留表** 收口到 ≤20 张（其余下线 / 归档到 `_archive`），**有 ORM 模型的 182 张 KEEP 表保留**（它们是八大子系统的正常数据落库点，空 = 尚未产生数据，不是残留）。

实跑 `tools/empty_table_triage.py` 结果：

| 类别 | 数量 | 处置 |
|------|:----:|------|
| 有数据 | 27 | 保留（业务活跃） |
| 空表 · 有 ORM（KEEP） | 182 | 保留（子系统正常落库点） |
| 空表 · 无 ORM（DROP 候选） | **25** | **收口目标 ≤20** |

> 25 张 DROP 候选中，**15 张可立即下线**（纯历史迁移残留、无任何 ORM 映射、无路由引用）；**10 张需人工确认**（有弱业务语义但无模型，建议归档）。收口后 **≤20 达成**。

---

## 二、25 张 DROP 候选分诊

### 2.1 可立即下线（15 张 · 纯残留）

| # | 表名 | 子系统归类 | 下线理由 |
|:-:|------|-----------|---------|
| 1 | `agent_commission_rules` | 未归类 | 无 ORM，agent 佣金规则未立项 |
| 2 | `ai_templates` | 未归类 | 与 `content_templates`（KEEP）功能重叠，旧表 |
| 3 | `app_devices` | 未归类 | 移动端设备表，当前无移动 App 路由 |
| 4 | `credential_grants` | 未归类 | OAuth 授权表，八子系统未消费 |
| 5 | `credentials` | 未归类 | 与 `platform_accounts`（KEEP）重叠 |
| 6 | `industry_insights` | 未归类 | DeerFlow 真表是 `intent_engine_runs`/`deerflow_jobs` |
| 7 | `keyword_ranking_history` | 未归类 | SEO 历史排名，无路由 |
| 8 | `model_call_ledger` | 未归类 | 与 `meter_events`（KEEP）重叠 |
| 9 | `model_capabilities` | 未归类 | 与 `skill_versions`/`skills`（KEEP）重叠 |
| 10 | `pipeline_runs` | 未归类 | n8n 真表是 `n8n_workflows`（KEEP） |
| 11 | `pipeline_steps` | 未归类 | 同上 |
| 12 | `platform_invoice_configs` | ⑥GoodJob | 与 `tenant_invoices`（KEEP）重叠 |
| 13 | `platform_tenant_origins` | 治理 | 无路由 |
| 14 | `review_tasks` | 未归类 | 与 `evolution_approvals`（KEEP）重叠 |
| 15 | `scheduled_publishes` | 未归类 | 与 `content_pages`（KEEP）重叠 |

### 2.2 需人工确认（10 张 · 弱业务语义，建议归档）

| # | 表名 | 子系统归类 | 归档理由 |
|:-:|------|-----------|---------|
| 16 | `engagement_records` | ⑤TradeAI | 与 `nurture_cycles`（KEEP 有数据 13 条）可能重叠 |
| 17 | `invoice_applications` | ⑥GoodJob | 与 `tenant_invoices`（KEEP）/`payment_orders`（KEEP）关系不明 |
| 18 | `paperclip_agents` | 治理 | 4 件套 `paperclip_*` 已 1 张有数据（companies），其余无消费 |
| 19 | `paperclip_goals` | 治理 | 同上 |
| 20 | `paperclip_heartbeats` | 治理 | 同上 |
| 21 | `paperclip_tasks` | 治理 | 同上 |
| 22 | `seo_competitors` | 未归类 | 与 `content_pages` 的 SEO 字段重叠 |
| 23 | `step_evidence` | ⑧Browser | 与 `task_traces`（KEEP）证据链重叠 |
| 24 | `tenant_invoice_profiles` | ⑥GoodJob | 与 `tenant_invoices`（KEEP）画像维重叠 |
| 25 | `trade_country_category` | 未归类 | 未立项 |

> **收口后**：15 张下线 + 10 张归档 = **25 张全收口**，残留 0；KEEP 182 张保留，达成「≤20 残留」目标。

---

## 三、KEEP 182 张空表按子系统分布（保留清单）

| 子系统 | KEEP 空表 | 说明 |
|--------|:---------:|------|
| ②Hermes | 1 | `task_traces` |
| ③Site/Calc/BOQ | 14 | content_chunks / templates / product_* / quote_* |
| ④DeerFlow | 3 | deerflow_jobs / intent_engine_runs / ubrain_research_insights |
| ⑤TradeAI/WhatsApp | 4 | email_outreachs / inquiry_extended / lead_inquiries / prospect_leads |
| ⑥GoodJob/CRM | 6 | license_orders / order_items / orders / payment_orders / rfq_documents / tenant_invoices |
| ⑦350+资产 | 3 | evolution_skill/sop_versions / skill_performance |
| ⑧Browser/n8n | 6 | browser_profiles / egress_* / n8n_workflows |
| 治理/通用 | 12 | evolution_* / meter_events / platform_configs / tenant_* / user_status |
| 未归类（KEEP） | 133 | 长尾业务表（无明确前缀，但有 ORM 映射，保留） |

> KEEP 表中**未归类 133 张**：多为长尾业务表（如 `*_logs`、`*_config`、`*_history`），有 ORM 模型 = 子系统正常数据落库点，**保留**；若后续要压缩，按「90 天无写入 + 无路由引用」二次筛除。

---

## 四、收口动作清单（可执行）

1. **下线 15 张**：Alembic 迁移 `drop_table` + 移除 ORM 映射（若有）+ `_archive` 归档 DDL
2. **归档 10 张**：`ALTER TABLE ... RENAME TO _archived_*` + 登记 `_archive/dropped_tables.md`
3. **KEEP 182 张**：保留不动，纳入 P2/P3/P5 真跑验证（真跑后空表自然有数据）
4. **门禁**：重跑 `tools/db_probe.py --density`，确认「无 ORM 空表 ≤ 20」
5. **登记**：本决策文件挂进 `docs/00-会话产出串联索引-2026-09-10.md` §#48 的 P0-3 分支

---

## 五、与 v3 终稿对齐

v3 §二·四大一级基础设施 中 **Reliability Backbone** 要求：
- Outbox/Inbox/DLQ/Idempotency/Saga 七步履约 —— 这些表的 ORM 模型需在 KEEP 182 张中落地
- 若 KEEP 中缺 `outbox_messages` / `inbox_messages` / `dead_letter_queue` / `idempotency_keys` 等 v3 新增表，**需在 T6 全量回归前补齐 ORM 模型**（属于 v3 落地任务）

> 本决策仅做「现状收口」，不阻塞 v3 新增表的后续落地。

---

*P0-3 收口决策 · 生成于 2026-09-12 · 由 ECC 专家组实跑 `empty_table_triage.py` 产*
