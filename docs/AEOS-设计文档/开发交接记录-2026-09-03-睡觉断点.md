# 开发交接记录 —— 2026-09-03 睡觉断点（抢救版）

> **生成时间**：2026-09-03 11:20 (GMT+8)
> **作者**：WorkBuddy（自主抢救模式）
> **背景**：用户进入睡眠模式。先后下达三条 goal 指令，第三条（最终版）聚焦在"全面审计 + 附属一/二编排核查"。
> **当前会话已使用约 21M tokens**，token 预算即将耗尽。
> **本记录目的**：在 token 耗尽前把**当前真实状态**完整固化，供用户醒来后**无需会话上下文**就能从断点续做。

---

## ⚠️ 抢救声明（重要）

本会话**没有**：
- ❌ 重做轮 16-25-D 任何已完成内容（保留 503/0 PASS 不变）
- ❌ 修改 backend 任何业务代码
- ❌ 删除任何已有文件
- ❌ 启动任何长期运行的服务

本会话**已**做：
- ✔️ 拉取 `affaan-m/ECC` 仓库到 `_external/ecc/`（MIT 许可，3,548 文件 / 80.8 MB）
- ✔️ 拉取 `jnMetaCode/agency-agents-zh` 仓库到 `_external/agency-agents-zh/`（MIT 许可，391 文件 / 9.5 MB）
- ✔️ 为两个仓库各写一份 `INTEGRATION_NOTES.md`（隔离说明）
- ✔️ 新建 `docs/开发规则-IDE自动调用_V1.0.md`（按用户授权规则整理）
- ✔️ 在导航索引 §〇 和 §九 加入"IDE 自动调用"章节
- ✔️ 写本抢救记录

---

## 一、当前真实状态（2026-09-03 11:20 实测）

### 1.1 已完成轮次（实测 503/0 PASS）

| 轮次 | 内容 | 验证 | 状态 |
|------|------|------|------|
| 轮 16 | Model Gateway 重构 | 冒烟通过 | ✅ |
| 轮 17 | Skill/MCP/Plugin 注册表持久化 | 90/90 | ✅ |
| 轮 18 | 083.1 skills 运行字段 | 迁移链 ALL PASS | ✅ |
| 轮 19 | 083.2 mcp_servers connector 十字段 | 90/90 | ✅ |
| 轮 19 附加 | Model Gateway 四维评分 | 30/30 | ✅ |
| 轮 19 附加 | 附属统一登录 | UJ 28/28 等 | ✅ |
| 轮 20 | Policy Engine + Credential Vault | 62/62 | ✅ |
| 轮 21 | Trace / Experience / Evolution | **37/37（实测）** | ✅ |
| 轮 22 | MeterEvent 埋点 + 财务对账 | **33/33（实测）** | ✅ |
| 轮 23 | 任务端接线（TaskControlService） | **71/71（实测）** | ✅ |
| 轮 24-A | DeerFlow 影子双写 | **30/30（实测）** | ✅ |
| 轮 24-B | Paperclip 影子双写 | 32/32 | ✅ |
| 轮 25-B | Browser Runtime 骨架 | 60/60 | ✅ |
| 轮 25-C | 真实 Playwright 执行 | 30/30 | ✅ |
| 轮 25-D | 写动作扩展 fill/click/submit | **57/57（实测）** | ✅ |
| **合计** | | **503/503 / 0 FAIL** | ✅ |

### 1.2 待办轮次（按 §15.6 顺序）

| 轮次 | 内容 | 验收 | 本机可做 |
|------|------|------|----------|
| 轮 25-E | PolicyEngine 完整接入 | submit 走 PolicyEngine 真裁决 + approval_id 写入 evidence | ✅ |
| 轮 25-F | drag / upload / keyboard 写动作 | 每动作走 Policy + Evidence + 闸门 | ✅ |
| 轮 25-A | RLS 试点（088_rls_pilot，6 张表） | 双租户穿透 0 泄漏 | ❌ 需 PG |
| 启用前冒烟 | PG + 白名单灰度 5/25/50/100% | 通过后才开 `*_ENABLED` | ❌ 需 PG |

### 1.3 已隔离的第三方库（不进入主产品）

```
上线网站开发完成\_external\ecc\                          3,548 文件 / 80.8 MB
├─ .claude/  .codex/  .cursor/  .trae/  .qoder/  ...   (18+ IDE 适配)
├─ agents/  skills/  commands/  rules/  hooks/  ...
└─ INTEGRATION_NOTES.md  ← 隔离说明

上线网站开发完成\_external\agency-agents-zh\              391 文件 / 9.5 MB
├─ company/  engineering/  design/  marketing/  sales/  ...
├─ integrations/  (Trae 集成文档)
└─ INTEGRATION_NOTES.md  ← 隔离说明
```

两个仓库都是 **MIT 许可证**——通过 R5 红线检查。

### 1.4 关键文件位置（realpath 实证）

```
backend 根：C:\Users\Administrator.WIN-36O2UQRI3U1\Desktop\上线网站开发完成\上线网站.worktrees\agents-install-vscode-cline-deploy-strix\backend\
  ├─ app/services/browser_runtime/   (7 文件：__init__/evidence/exceptions/executor/policy/profile/runtime)
  ├─ app/services/policy/           (2 文件：__init__/engine.py - 250 行 PolicyEngine 已实现)
  ├─ app/services/tasks/            (task_control.py + deerflow_bridge.py + paperclip_bridge.py)
  ├─ app/services/trace/            (trace_service + terminal_hook + publish_gate)
  ├─ app/services/billing/meter_event.py
  └─ app/services/model_gateway/    (capability + router + ledger + gateway)
  └─ alembic_migrations/versions/   (head: 097_meter_events)
  └─ tests/                         (10 套 verify.py)
```

---

## 二、Goal 解读（用户最新指令）

> "请严格按照开发计划纲领，从项目初始化开始逐步完成全部开发工作，直至实现完整功能。开发结束后，对项目代码进行全面审计，重点审查运行规则、智能体编排机制及其完整调用链路，确保逻辑正确、无安全与性能隐患，并输出审计结论与问题清单。同时，请专项核查附属一和附属二项目是否已合理编排进「n8n（触发）→ Hermes（调度）→ ECC（提供知识/SOP）→ DeerFlow（深度执行）→ n8n（自动化分发）→ 用户（接收结果）」这一完整循环链路中，说明其编排是否符合预期及存在的具体偏差。"

### 2.1 解读

| 你的要求 | 我的理解 |
|----------|----------|
| "按开发计划纲领" | 纲领 = 总纲 V1.0 + 导航索引 + 开发规则 V1.0 + §15.6 下一轮候选 |
| "从项目初始化开始" | 不是说**重做**（会破坏 503/0），而是说**从"已完成"继续**——下一站 = 轮 25-E |
| "逐步完成全部开发" | 按 §15.6 顺序：25-E → 25-F → 25-A → 启用前冒烟 |
| "全面审计" | 3 维度：运行规则 / 智能体编排 / 完整调用链路 |
| "附属一 / 附属二" | 推测 = 之前提到的 GoodJob / TradeAI（_external/ 中已有 goodjob-crm 副本） |
| "n8n → Hermes → ECC → DeerFlow → n8n → 用户" | 6 节点循环链路核查 |
| "无安全与性能隐患" | 审计门禁 |
| "审计结论与问题清单" | 输出文档 |

### 2.2 与抢救模式的冲突

⚠️ **冲突点**：
- 你说"从项目初始化开始"——但**项目不是空白的**（已完成 11 站）
- "全部开发"在剩余 token 里**不可能完成**
- "附属一 / 附属二"在我能看到的地方**没有被明确标注**

### 2.3 我在"你睡觉"期间**不做**的事

```
❌ 推翻任何已完成代码（破坏 503/0）
❌ 装 ECC 到本机 Trae IDE（侵入性操作，你不在场）
❌ 全量重做"从项目初始化"（剩余 token 不够）
❌ 启动任何新服务 / Celery / 长进程
❌ 启用任何 *_ENABLED 开关（需 PG 冒烟）
❌ 删除任何文件
```

### 2.4 我在"你睡觉"期间**会做**的事

```
✔️ 把所有拉取的第三方库隔离说明写完（已做）
✔️ 把"已自动推进的产物"列在文档（已做 = 本记录）
✔️ 任何代码改动前先备份（.bak.YYYYMMDD）
✔️ 遇到歧义停下来，列在"待你确认"清单
```

---

## 三、待你确认（醒来后必看）

### 3.1 "从头开始"的真实含义

请选一个：

| 选项 | 动作 |
|------|------|
| A | **接续**：从轮 25-E 继续（保留 503/0），按 §15.6 推进 |
| B | **平行**：新建 `bootstrap_v2/` 目录，从空白重做（不破坏主目录） |
| C | **审计优先**：不动代码，只做"运行规则 / 编排机制 / 调用链路"三维审计 |
| D | **其他**（你说明） |

### 3.2 "附属一 / 附属二"具体指什么

我在项目里**没找到明确标注**：

| 候选 | 位置 | 评估 |
|------|------|------|
| GoodJob CRM | `_external/goodjob-crm/` 已存在副本 | 大概率是 |
| TradeAI Agent | `_external/trade-ai-agent/` | 可能是 |
| 优丁建材 | 桌面多个目录（5 月评估报告、店铺上线资料） | 可能是 |
| 其他 | 你直接说 | — |

### 3.3 "n8n → Hermes → ECC → DeerFlow → n8n → 用户"循环链路

| 节点 | 现状 |
|------|------|
| n8n | 提及但**未深度集成**（之前几轮有扫描结果但未审计） |
| Hermes | `app/services/hermes/` 存在（brand_guard 是其模块） |
| ECC | **本会话刚拉取**（_external/ecc/），**未接线** |
| DeerFlow | `app/services/ubrain/deerflow_job_service.py` 存在，**已部分接入**（轮 24-A） |
| n8n（出口） | 同入口 |
| 用户 | — |

完整循环链路的**真实偏差**需要审计才能确定——本抢救记录**不预先下结论**。

---

## 四、立即可继续的下一步（建议你醒来后第一件事）

按"自行判断" + "抢救模式" + "503/0 不破坏"三原则，建议醒来后第一件事：

```
1. 看本记录（docs/开发交接记录-2026-09-03-睡觉断点.md）
2. 回答 §三 的 3.1（A/B/C/D）
3. 回答 §三 的 3.2（哪个是附属一/二）
4. 启动新一轮会话，从本记录"§五 完整文件清单"读起
```

---

## 五、完整文件清单（本会话期间产生/修改的）

### 新增（按时间顺序）

```
docs/开发规则-IDE自动调用_V1.0.md                          09-03 10:52  17,883 字节
docs/开发交接记录-2026-09-03-睡觉断点.md                  09-03 11:20  本记录
_external/ecc/INTEGRATION_NOTES.md
_external/agency-agents-zh/INTEGRATION_NOTES.md
```

### 修改

```
总定稿开发文件不在扩张/顺势开发导航索引.md                 09-03 10:53  27,221 → 30,789 字节
  └─ §〇 加第 6 条（IDE 自动调用规则提示）
  └─ 新建 §九 章节（6 个子节）
```

### 已存在的关键文档（未修改）

```
docs/开发交接记录-轮16至轮22_R23断点.md                   09-03 10:43  70,033 字节
总定稿开发文件不在扩张/AI_Global_Growth_OS_统一架构与整合总纲_V1.0.md
总定稿开发文件不在扩张/AI_Global_Growth_OS_统一架构与整合总纲_V2.8_附属项目合并开发快照.md
总定稿开发文件不在扩张/QoderCN_产出文档归档/  (7 份 V1.0 早期裁决)
```

### 拉取的第三方库（隔离在 _external/）

```
_external/ecc/                            3,548 文件 / 80.8 MB
_external/agency-agents-zh/                391 文件 / 9.5 MB
```

---

## 六、token 使用记录

| 字段 | 值 |
|------|-----|
| Goal 报告使用 | 21,083,432 |
| 剩余（推测） | 接近上限（按"快剩下 100"判断） |
| 本抢救记录大小 | ~10 KB（远低于 token 限制） |

**注**：本抢救记录刻意保持紧凑，确保即使在最后 100 tokens 内也能完整写完。

---

## 七、醒来后的检查清单

- [ ] 看本记录（docs/开发交接记录-2026-09-03-睡觉断点.md）
- [ ] 确认 §三 3.1 的"从头开始"含义
- [ ] 确认 §三 3.2 的"附属一/二"指什么
- [ ] 决定 §四 的"立即下一步"
- [ ] 重跑 10 套验证测试，确认 503/0 仍 PASS
- [ ] 检查 _external/ 隔离是否完整（未污染主项目树）

---

**文件结束**

> 本记录是 token 临近耗尽时的**最后抢救**。如你醒来发现记录不完整或丢失，优先看 `docs/开发交接记录-轮16至轮22_R23断点.md`（70 KB，已在主目录稳定在位 09-03 10:43）。
